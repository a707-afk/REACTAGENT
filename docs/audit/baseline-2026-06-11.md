# Phase 0 基线报告

> 日期: 2026-06-11
> 分支: feature/ecom-agent
> HEAD commit: d8c1300

---

## 1. 仓库状态

| 项目 | 值 |
|---|---|
| 分支 | feature/ecom-agent |
| HEAD | d8c1300 [Task8+9] load test benchmark |
| 未追踪文件 | 7 个临时脚本 + ENTERPRISE_RAG_AGENT_REBUILD_GUIDE.md + .codebuddy/ |
| 修改中文件 | 无 |

## 2. 编译检查

```
python -m compileall -q app
EXIT_CODE=0
```

**结果: 通过**，所有 Python 文件编译无错误。

## 3. 测试检查

```
python -m pytest -q tests/
EXIT_CODE=2 (collection error)
```

**结果: 失败** — 2 个 import error，0 个测试被执行：

| 测试文件 | 错误 | 根因 |
|---|---|---|
| `test_domain_router_embedding_mock.py:14` | `ImportError: cannot import name '_legacy_route' from 'app.domain_router'` | `_legacy_route` 函数在重构时被移除 |
| `test_llm_zhipu.py:3` | `ModuleNotFoundError: No module named 'app.llm_zhipu'` | `app.llm_zhipu` 模块已不存在，已改为 `app.llm` |

### 已确认的过期引用

- `test_domain_router_embedding_mock.py` 引用 `_legacy_route` — 代码中不存在
- `test_llm_zhipu.py` 引用 `app.llm_zhipu` — 模块已移除
- 代码中 `app.llm` 是当前正确引用（`from app.llm import chat_completion`）
- `app.domain_router` 只导出 `route_domains` 和 `RouterResult`

## 4. 前端构建

```
npm --prefix frontend run build
EXIT_CODE=0
✓ built in 676ms
```

**结果: 通过**

产物:
- `static/app/index.html` (0.42 kB)
- `static/app/assets/index-BR6fCUuy.css` (1.82 kB)
- `static/app/assets/index-CnV3ya6S.js` (146.78 kB)

## 5. Docker Compose 配置验证

```
docker compose config
EXIT_CODE=0
```

**结果: 配置合法但有过期/不一致**

| 问题 | 严重度 | 说明 |
|---|---|---|
| `ZHIPUAI_API_KEY` 仍存在 | 高 | 行22，实际项目已不用智谱 API，改用 SenseNova |
| `QDRANT_PATH=/data/qdrant_local` | 高 | 让 app 用嵌入式 Qdrant 路径，而非连接 qdrant 服务（端口6333） |
| 无 Redis 服务 | 高 | 无法实现队列、限流、分布式缓存 |
| 无 worker 服务 | 高 | 长任务和摄入无法异步化 |
| 无 upload 相关配置 | 中 | 无文件上传能力 |

## 6. 已确认的核心代码缺陷

### 6.1 工单 API 使用内存字典

- 位置: `app/api/tickets.py:19-20`
- 代码: `# In-memory store for demo (replace with DB queries in production)` + `_tickets: dict[str, dict[str, Any]] = {}`
- 影响: 服务重启数据丢失，无并发控制，无审计

### 6.2 Worker 节点绕过 RAG 质量链路

- 位置: `app/agent_graph/graph.py:92`
- 代码: `# All worker nodes go directly to draft (skip retrieve/gate/grader pipeline)`
- 影响: exchange_parallel, refund_flow, complaint_flow, tracking_flow 全部跳过 gate/grader

### 6.3 Worker 自造 evidence

- 位置: `app/agent_graph/nodes.py:594, 631, 646, 662, 702`
- 模式: `retrieved_chunks: [{"text": <Worker自己生成的回复>, "score": 1.0, ...}]`
- 影响: grounding 检查对 Worker 路径无意义，是自证

### 6.4 Alembic 迁移为空

- 位置: `alembic/versions/` 目录为空
- 影响: 无数据库迁移，无法建表

### 6.5 CI 忽略核心测试

- 位置: `.github/workflows/test.yml:48`
- 代码: `--ignore=tests/test_agent_graph_compile.py`
- 影响: Agent 核心测试被跳过

### 6.6 CI 仅对 main 触发

- 位置: `.github/workflows/test.yml:5-7`
- 代码: `branches: [main]`
- 影响: feature/ecom-agent 分支的 PR 和 push 不触发 CI

## 7. 临时脚本审计

| 文件 | 行数 | 内容 | 判定 |
|---|---|---|---|
| `block_start.txt` | 4 | CCX 代理配置 scope | 与本仓库无关 |
| `ccx_replacer.py` | 157 | 替换 Codex MEMORY.md | 与本仓库无关 |
| `check_boundaries.py` | 15 | 检查 Codex MEMORY.md | 与本仓库无关 |
| `fix_memory.py` | 35 | 修复 Codex MEMORY.md | 与本仓库无关 |
| `mem_replace.py` | 7 | 替换 Codex MEMORY.md | 与本仓库无关 |
| `sum_updater.py` | 51 | 更新 Codex memory_summary | 与本仓库无关 |
| `new_block_part1.txt` | 14 | CCX 代理配置 scope | 与本仓库无关 |

**判定**: 全部为 Codex 环境管理脚本，与本仓库功能无关，应删除。

## 8. 配置不一致

| 项目 | 当前值 | 应改为 |
|---|---|---|
| `app_name` | `cs-agent-backend` | `ecom-agent` |
| README LLM | "Zhipu GLM-4-Flash" | "SenseNova DeepSeek-V4-Flash" |
| README 数据库 | "SQLite (dev) / PostgreSQL 16 (prod)" | 需标注 "tickets API 当前为内存字典，未落库" |
| README 图 | "54 nodes" | 实际已有 106 vectors |

## 9. 基线评估

按审计指南打分：

| 维度 | 评分 | 说明 |
|---|---:|---|
| RAG 检索基础 | 45 | 有混合召回、rerank、gate，但数据规模小，评测不稳定 |
| Agent 能力 | 20 | 固定路由和脚本化工具调用，Worker 绕过质量链路 |
| 企业后端 | 15 | 模型有，接口未落库，迁移为空 |
| 文件/多模态摄入 | 5 | 基本没有 |
| 安全与权限 | 35 | 有规则拦截雏形，缺少生产鉴权 |
| 降级与容灾 | 25 | 有少量 LLM fallback，缺系统级故障策略 |
| 评测与 CI | 20 | 测试 collection 失败，CI 忽略核心测试 |
| 部署与运维 | 25 | 有 Docker 壳，缺 Redis/worker/migration/health gating |

**综合评分: ~28/100**

---

**此报告为真实基线。后续 AI 不应再引用旧的 15/15 mock 评测当当前结果。**
