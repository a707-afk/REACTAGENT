有个做Go开发的朋友，上周跟我吐槽。

他面了15家公司，字节、腾讯、阿里、美团都走了一圈。HashMap、GMP模型、Redis持久化这些传统八股文，他背了三个月，几乎没卡壳。算法题也刷了两百多道，Medium难度的现场手撕基本能过。

结果呢？技术面全挂。

说实话我听到这个结果是有点意外的。六年经验，四年Go深耕，八股和算法都没问题，怎么可能15家全挂？他跟我说了细节之后，我才意识到2026年后端面试的水位线，早就悄悄抬高了。

问题出在哪？他告诉我一个特别有意思的细节。

字节三面的时候，技术总监问了这么一句。

"现在我们的系统开始引入AI Agent做业务决策，在多Agent协同的场景下，Go的内存模型和并发优势怎么支撑这类数字劳动力？如果出现服务过载，你的降级策略是什么？"

他当时就愣住了。

因为他准备的全是Redis过期策略、Kafka高吞吐原理、线程池参数调优这些经典八股。AI Agent的工程化落地？他连简历上都没敢写，面试官却追着问了十五分钟。

用他自己的话说：拼过了八股文，干掉了算法，最后却在AI上翻了船。

这哥们是工作六年的老后端了，过去四年一直在深耕Go生态。他自嘲说，以前面试核心竞争力是微服务架构和性能调优，现在发现这些东西已经变成了基础门槛，面试官真正想聊的早就换了一套试卷。

这事给我挺大触动的。因为我辅导过的后端学员里面，跟他情况类似的不在少数。八股文背得滚瓜烂熟，项目经验也能讲出花来，但一聊到AI工程化就露怯。不是能力不够，是备考的方向压根就不对。

2026年的后端面试，到底变了什么
我把最近搜到的面经数据汇总了一下，发现一个很明显的趋势。

从2025年下半年开始，大厂后端面试的考察重心发生了偏移。传统的Java基础、MySQL索引优化、Redis数据结构这些依然会问，但占比在明显缩小。取而代之的是三个新的考察方向。

说实话，这个变化来得比大多数人预想的要快。

第一，RAG系统的工程化设 计

不再是问Embedding是什么这种概念题。面试官会直接把一个业务场景抛给你：比如你们公司有一个客服知识库，用户提问口语化很严重，怎么设计检索策略才能保证召回率？如果检索结果不准，有没有多级补偿方案？文档切片策略怎么设计？

这些问题考察的不是你知不知道RAG，而是你有没有真正把RAG从一个Demo做成过生产环境可用的系统。

第二，Agent系统的架构能力。

字节和阿里今年的后端面试里，Agent相关的追问频率高得惊人。字节二面甚至会直接问，如果设计一个金融数据分析的Agent，它的记忆系统该怎么设计才能保证长期对话的准确性？

阿里则更关注工程架构。比如让你对比LangChain的Chain编排和字节Eino框架的Graph编排，在Go语言生态下各自的优劣是什么？

这些问题背后考察的是你对Agent核心组件的理解：Planning、Memory、Tools、Action。不是知道定义就行，而是要知道在实际工程里怎么落地。

AI应用中的工程挑战。

这个方向问得最实际。比如LLM调用失败率多少？怎么处理的？Prompt版本管理怎么做？如何设计AB测试？敏感信息怎么脱敏？

腾讯有个面试官问得特别狠："如果你的AI服务突然挂了，下游依赖它的十几个接口全部受影响，你的熔断和降级策略是什么？"

这本质上是一个经典的高可用问题，但套上了AI工程化的壳。如果你只会背Hystrix的原理，却从来没想过LLM服务的特殊性，比如Token限流、响应超时怎么处理、模型切换怎么无缝衔接，这个问题根本答不到点子上。

后端面试开始问AI了
有朋友可能会问，我做后端开发的，为什么要懂 A I？

这个问题本身就已经暴露了一个认知误区。

2026年的后端岗位需求结构，正在发生一个很剧烈的分化。我查了一下华为云创想者大会的数据，普通后端开发岗位需求同比下降了52%，但AI智能体开发工程师这个方向的招聘量却在激增。

注意，不是AI算法工程师，是AI工程化方向的岗位。企业真正缺的人，是能把AI能力集成到现有后端架构里的工程师。

举个实际的例子。你们公司的客服系统，以前是一个传统的规则引擎加人工坐席。现在老板要求接入大模型做智能客服。这个需求落到工程团队头上，后端工程师要面对的是一整套工程挑战：

怎么设计Prompt的版本管理和灰度发布？
LLM接口的延迟和稳定性怎么处理？要不要做本地缓存？
用户对话的上下文怎么维护？Session存在哪？
敏感数据怎么脱敏？合规要求怎么满足？
多轮对话出错的时候怎么回滚？用户体验怎么保证？

这些问题没有一个属于传统八股文的范畴，但每一个都是后端工程师在2026年必须面对的真实挑战。说白了，老板不会关心你用的是什么模型，他关心的是这个系统能不能稳定跑起来、出问题了你修不修得了。

准备 方向不对，努力全是白费
回到我那个面了15家公司全挂的朋友。

他后来花了半个月重新调整了准备策略，重点做了一件事：把自己项目里的一个数据同步模块，改造成了一个带RAG能力的智能查询Agent。虽然是个内部工具，但他把整个架构设计、踩坑过程和优化数据都整理得清清楚楚。

然后他去面第16家，一家深圳的独角兽公司。

这次面试官问他RAG相关的问题，他直接把项目的架构图画了出来：怎么接入Milvus做向量检索，怎么设计混合检索策略，怎么解决文档切片的边界问题，召回率从多少优化到了多少。

面试官当场就表示这个思路很扎实。

他拿到了Offer。薪资比他预期还高了15%。

他告诉我一个特别关键的感悟。

2026年后端面试的本质没有变，面试官依然是在考察你解决工程问题的能力。但问题的边界变了。以前的问题是"怎么让一个系统高可用"，现在的问题是"怎么让一个AI系统高可用"。

AI系统的高可用，跟你熟悉的传统系统高可用，底层方法论是一样的。但执行细节完全不同。比如传统系统的熔断看的是QPS和响应时间，AI系统的熔断还要看Token消耗和模型可用性。

底层逻辑完全一样，只是上面盖的那层业务场景换了个壳。

三个立刻能做的准备动作
如果你正在准备后端面试，我的建议是立刻调整备考重心。别再把所有时间都砸在八股文上了，至少留三分之一的精力给AI工程化这个方向。

第一，吃透至少一个AI工程化框架。

不要泛泛地了解LangChain、LlamaIndex这些概念。选一个你最熟悉的技术栈，深入理解它的工程实现。如果你是Java后端，就去看看Spring AI Alibaba的设计思路；如果你是Go后端，就研究一下Eino框架的Graph编排和并发模型。

面试官问框架，不是在考你API怎么用，是在考你工程判断力。

第二，准备一个能讲深的AI项目。

不需要多复杂，但要有真实的技术决策过程。比如你是怎么选型向量数据库的？Milvus、Pinecone、Chroma各自的优势是什么？你的场景为什么选了这个？

重点是展示你的决策能力，不是展示你调包的能力。

把AI工程化和传统后端能力打通。

这是最核心的准备思路。不要割裂地准备两套知识体系。你熟悉的高并发、分布式事务、熔断降级这些能力，在AI工程化场景下一样适用。关键是你要能灵活地把它们迁移到新的业务场景里。

比如面试官问你LLM服务的降级策略，你可以用你熟悉的Sentinel原理来回答，但要在回答里体现出你对LLM服务特殊性的理解。这种打通能力，才是2026年后端面试真正的分水岭。

写在最后
2026年后端开发的方向，不是没机会了。是门槛变了，但机会也跟着变了。

传统的八股文和算法题依然是基础，但只靠这些已经不够了。企业真正想要的，是既有扎实后端功底、又懂AI工程化落地的复合型人才。

这个 变化对后端工程师来说其实是好事。因为AI工程化的核心挑战，本质上还是工程挑战。延迟怎么优化、稳定性怎么保障、高可用怎么设计、数据一致性怎么维护，这些后端工程师最熟悉的战场，现在只是换了一个更前沿的战场而已。

而对于那些能在这个新战场上站稳脚跟的人来说，薪资和机会都远超传统后端岗位。我身边有后端朋友转了AI工程化方向，薪资涨幅基本都在30%以上。



前几天一个学员发来他的面试录音。

我戴上耳机一个人坐在那听，听着听着就停住了，盯着那个进度条发了好一会儿呆。每个技术点他确实都答到了面上，LangChain用过，RAG知道流程，Prompt技巧也攒了一堆。但就是给人一种感觉，他在背答案。

面试官追问了两个问题之后，他开始东拉西扯了。

这不是他一个人的问题。最近我连续复盘了十几个AI全栈方向的面试，发现一个规律。所有人挂掉的原因都一样，不是技术不行，是他们准备的方向跟面试官想听的，完全是两码事。

AI全栈到底什么概念

说实话，很多人对AI全栈的理解太表面了。

觉得就是前端加后端，再会用Python调几个大模型API，齐活了。不是的，差太远了，我跟你说。

AI全栈真正的意思，是你拿到一个产品需求，能独立把一个AI应用从0到1做出来、部署好、持续迭代下去。技术选型、架构设计、AI集成、性能优化、生产部署，每一步都得有判断力。

我自己琢磨了一下，这东西有三层。

第一层，传统全栈能力。前后端加数据库，能搭系统。这是基本盘。

第二层，AI工程化能力。RAG、Agent、Prompt Engineering、向量数据库、模型部署。这些是AI应用开发的核心武器。

第三层，产品判断力。知道什么时候该用AI，什么时候不要硬用。知道LLM擅长解决什么问题，不擅长解决什么。

我辅导的学员里，能过面试的，都是第三层有自己想法的。大部分人前两层皮毛沾了点，第三层完全没意识。面试官随便追问一个问题，立马接不住了。

2026年市场到底要什么样的人

先看两组数据，你感受一下。

猎聘和BOSS直聘的统计，2026年AI岗位的薪资中位数比传统开发高了20%到35%。脉脉的春招报告更夸张，AI相关岗位的需求量暴涨了12倍。

岗位是真的多，钱也是真的多。

但有意思的是另一面。我帮学员复盘的时候发现，很多人明明拿到了面试机会，就是过不了。问题出在哪，出在准备的方向上。

大部分人怎么准备面试呢。看看LangChain文档，把LlamaIndex的tutorial跑一遍，攒一堆Prompt技巧。然后面试的时候，对方一问细节就开始含糊了。

企业要的不是学过什么的人，是做过什么、想清楚过什么的人。

给你还原一下真实面试场景。

面试官问RAG流程。你回答：文档切割、向量化、检索、生成。这个回答没问题，但只是入门版。他下一句就问，你的chunk size怎么选的，overlap设了多少，为什么这么设。碰到检索结果跟问题无关的情况怎么处理。

就这一句话，你前面所有准备，一下就到底了。

面试官到底想听什么

还是回到前面的例子。

那个学员面试的时候被问到Function Calling。他的回答是，LLM调外部API，可以查天气发邮件那些。

面试官听完皱了皱眉，没追究，换了下一题。

后来我让他复盘，我说你的回答从字面上看没错，但是停在了一个很浅的层面。Function Calling真正厉害的地方，不是LLM多了一个调API的能力，而是它让LLM变成了能自主做决策的Agent。LLM自己判断用户想干嘛，自己决定调哪个函数、传什么参数。这不是简单的转发，是机器开始有判断力了。

同一个知识点，两个人说出来，差距就是这么明显。一个停留在知道的层面，一个到了理解的层面。

面试不考你知不知道，考你想没想清楚。考你有没有做了以后认真复盘、踩过坑、形成自己的判断。

最被低估的竞争力

RAG项目现在基本上是AI应用岗的标配了。

但说句实话，大部分人的RAG项目，面试的时候经不起三个追问。

我列几个问题，你试着在心里答一下。

chunk size你设了多少，为什么是这个数不是别的数。embedding模型怎么选的，对比过哪几个。有没有做rerank，为什么做了或者没做。检索到一些跟问题无关的内容，你怎么兜底。

这些不是八股题，是真实项目里一定会碰到的问题。你真正做过，每个问题你都能讲出道理来。你只是跑了个教程，这几问一个问题也答不扎实。

还有一个被很多人忽略的细节，引用溯源。

大多数RAG项目做到检索加生成就停了。但如果你在回答结果里标注了来源，来自哪个文档、第几页，面试官的判断立马不一样。他会觉得这个人不是抄的教程，是真的把项目做完整了。这个细节实现成本极低，效果却好得惊人。

最高效的准备路径

从我自己的经验来看，优先级是这样的。

项目大于证书大于简历包装。

先把项目做出来。RAG是最合适的切入点，技术栈清晰，完整度可控，面试能聊的点够深。

做完项目以后别急着写简历。先把自己的思考过程完整过一遍。为什么选这个技术栈，遇到什么问题怎么解决的，做完以后最大的收获是什么。把这些想清楚，简历自然有内容可写。

简历里不要只列技术栈，把你做了哪些选择，为什么这样选，结果怎么样，写进去。

面试前花一个小时把项目从头到尾再过一遍。把自己当面试官，想想哪些地方会被追问。提前准备好，不要临场现编。

AI全栈这个方向，现在确实是最好的时间点。岗位多、薪资高、但真正有深度的人少。市场还在扩张、还没有饱和。你方向对了，比别人多想一步、多做一步，拿到结果是早晚的事。







前段时间一个做AI应用开发的学员跟我聊，说他们团队要做知识库，问我embedding模型怎么选。他说他们领导觉得直接看MTEB排行榜选个分数最高的就行。

我说千万别，我去年也是这么干的，结果做完测试整个人都愣了。

我们做的电力行业知识库，上线前我找了一套自己标注的300条行业问答对做了测试。BGE-M3在排行榜上分数不低吧，1024维568M参数，结果放到真实数据上一测，搜变压器瓦斯保护动作处理流程，前五条结果里只有一条是相关的，剩下四条全是别的设备的规程。

排行榜测的是新闻百科对话这些通用场景，你的业务越垂直越专业，排行榜就越靠不住。

不要信排行榜，要信自己的测试集

后来我换成bge-large-zh-v1.5，专门中文训练的那个。同样的测试集，准确率上来了一点。但你会发现一个有意思的事，有些查询这个模型强，换个查询又不如下一个了。比如搜保护装置怎么整定这种短的口语化表达，BGE-M3反而表现更好。不存在一个模型在所有场景下都碾压另一个。

我前前后后试了五六个模型，从text2vec到bge-large到Qwen到KaLM。最后测试最好的模型把检索准确率从70%拉到了77%，提升了7个百分点。但代价是推理速度慢了五六倍，部署占22G显存。这个账算下来其实没那么划算，因为微调bge-large加了rerank也能达到类似效果，成本低得多。

向量精度这个东西，掉进去就很难爬出来

Qwen3-Embedding出来的时候我挺兴奋的，2560维，排行榜上比BGE-M3高一截。结果第一步就翻车了。我们用的pgvector，向量类型硬上限2000维，2560写不进去。

我当时想降成halfvec，float32变float16不就好了吗。测试完发现检索准确率掉了5到8个百分点。这个损失比换模型带来的提升还大。

embedding模型的输出向量千万不能量化。每个维度看起来只损失一点点精度，但向量检索算的是余弦相似度，维度越高误差累积得越厉害。每层加上一点点浮动，几百个维度下来方向都偏了。我查了很多资料，这条建议在各种embedding选型指南里都出现过，但我没当回事，直到自己踩了才记住。

正确的做法是换向量数据库，Milvus或者Qdrant都原生支持高维度。或者用Qwen的Matryoshka特性手动指定输出1024维，精度至少是完整的，比量化靠谱得多。我后来用2560维加Milvus跑了一轮测试，准确率只提升了2个百分点。2个百分点的提升，换了数据库，慢了2.5倍，存储翻了2.5倍。不划算。

分库召回比换任何模型都管用

这一个经验我觉得比上面所有选型技巧加起来都重要。

我们的知识库里混了好几种不同类型的文档，运行规程、检修规程、设备说明书、安全规范。全部灌到同一个向量索引里。搜变压器差动保护整定，出来的结果混在一起，有运行规程的章节、有检修规程的章节、还有设备说明书里的参数表。用户看到结果的时候根本分不清哪个是自己要的东西。

这跟embedding模型没关系，是召回策略的问题。后来我改成按文档类型建不同的索引，检索前先用LLM判断查询属于哪个类型，再去对应的库里搜。同一个库里的文档上下文都是同一领域的，不会再出现不同规程混在一起的情况。这个方法不用换模型，不用加硬件，信噪比提升了一大截。

问题改写，被很多人忽略的关键环节

还有一个容易被忽略的点。用户问知识库一个问题，你直接拿这句话去检索，效果经常不好。因为用户的提问方式和知识库里文档的表述方式差距太大。

举个真实例子。用户问变压器嗡嗡响怎么办，我们的文档里写的是变压器异常声响识别与处理。用户的嗡嗡响和文档的异常声响，字面完全不匹配。语义上其实很接近，但余弦相似度不够高，排不进前五。

我们的做法是用LLM在检索前先做一次改写。不是简单同义词替换，是把口语化表达扩写成包含专业术语的描述。比如变压器嗡嗡响怎么办扩写成变压器运行异常声响识别，变压器正常运行声音特征，变压器异响原因分析及处理方法。用改写后的文本去索引里检索，召回率从40%直接拉到75%。

再比如保护老跳闸改写成继电保护装置频繁动作跳闸原因分析，保护装置误动作判别方法。召回率从35%拉到80%。

一条简单的prompt，一个LLM调用，延迟增加200毫秒，换来召回率翻倍。这个账怎么算都划算。

面试的时候，能聊出这些比背八股文有用多了

很多人面试大模型应用开发岗位，简历上写做过RAG项目，面试官一问embedding选型就开始背排行榜排名。你要是能说出MTEB排行榜跟真实场景的差距，讲出向量量化的坑，聊清楚分库召回和问题改写为什么比换模型管用，面试官会高看你一眼。这些是你的真实经验，不是从博客上看来的。

如果你刚开始做RAG项目

不要一上来就折腾大模型。先用bge-large-zh-v1.5跑一版基线，1024维，326M参数，pgvector原生支持。跑完你才知道瓶颈在哪。是模型不够好，还是文档没清洗，还是分块策略有问题。没有基线数据，换什么模型都是盲人摸象。

如果基线效果不够，先试微调和加rerank。标注几百条问答对做微调，加一个reranker做二次排序，这两个投入产出比远高于换大模型。微调能提升12个百分点，rerank能提升七八个。换KaLM-12B只提升7个点，但硬件成本翻了数倍，推理速度慢五六倍。

embedding选型在整个RAG系统里重要性大概排第四。排在前面的三个是文档预处理质量、分块策略、召回策略。这三个做好了，embedding用bge-large-zh就能跑出不错的效果。这三个没做好，你用最好的模型也救不回来。很多人在embedding选型上花太多时间反复对比排行榜上零点几的分数差，不如花在分库召回和清洗数据上。






两天有个学员来给我报喜，说offer下来了，AI Agent研发岗。

我挺替他开心的，因为他来找我的时候，情况真的不算好。物理专业，非科班，手里就一个跟着教程做的demo，简历投出去基本石沉大海。

后来我带他重新梳理了项目，从架构设计到工程落地，一条一条抠。他这个人执行力确实强，我说什么他回去就做，做完了回来跟我对，我再说问题，他再改。

三个月不到，offer来了。

今天想把他这个案例拿出来聊聊，顺便说说我这几年辅导AI方向求职看到的一些规律。


先说他的起点 是怎么样

物理专业， 去 年秋招准备找互联网开发岗，一开始走的是算法方向，刷了几个月题，感觉自己啃得很痛苦。岗位竞争太卷了，那批真正搞算法的同学背景都很强，他有点迷茫。

后来他自己在网上刷到了几篇AI Agent相关的文章，开始对这个方向有了兴趣。但那时候手里就一个小项目，项目讲得很散，面试被问起来答不上来。

我拿到他的简历，跟他聊了一轮，梳理出来两个核心问题。

第一，项目停留在demo层面，没有工程落地感。

第二，技术栈零散，看不出他 对Agent系统整体架构有什么理解。

这两个问题，是我见过的绝大多数转AI方向的同学都有的，不是他一个人的事。

先说第一个问题，什么叫工程落地感。

他最开始做的项目是一个聊天机器人，接了几个API，能问答，基本上绝大多数入门教程跟着走都能做出来的那种。

这个东西放在简历上，面试官看了一眼就知道了，你只是跑了别人的代码，没有真正解决过问题。

我让他重新整理实习期间做的那个内 容数据处理平台。这个平台每天要处理大量的结构化资讯数据，涉及爬虫、向量化存储、知识库查询、接口开发这几块。

我跟他一起把这个项目重新拆了一遍，哪些点值得写，哪些点要舍掉，量化数据怎么呈现。跑通之后每天的数据处理量大概在一两万条，知识库里积累下来有几十万条记录，爬虫的采集成功率跑到了95%以上。

这些 的数字，面试官一听就知道你真的跑过、调过、踩过坑。不是编的。

工程落地感的第一层，有量化，有规模，不 是demo玩具 项目。

光有量化还不够。

你得能 讲清楚你在系统里做了什么选择，以及为什么这么选。

他实习项目里向量存储这块用了Qdrant，而不是更常见的方案。我让他把这个点的表述从"我用了Qdrant"改成"在什么场景下Qdrant更合适，做多路检索融合的时候踩了什么坑、怎么解决的"。

面试官问到这里，是有点被勾住的。

因为很多背了一堆RAG八股文的同学，一碰到"你做的时候遇到了什么问题"这类追问就答不上来了。真的做过的和没做过的，一问就知道。

这个差距，大得很 明白吗 ，

第二个问题，架构理解

这是很多同学容易忽略的一块，也是我辅导的时候花时间最多的一块。

做AI Agent，不是把几个工具拼在一起就完了。面试官真正想看的，是你对整个Agent系统的调度逻辑有没有理解。

他后来做了一个多智能体系统的项目，这个项目我跟他从零开始设计架构，从单Agent改到多Agent，从串行改到并行，每一步我都在旁边盯着。

最终用了Planner-Coordinator-Specialist的多层架构，不是单个Agent做所有事，而是把任务拆开，规划层负责拆解目标，协调层负责分发任务，执行层负责具体操作，最后有一层负责汇总结果。

这个架构设计，跟现在很多大厂在用的生产级Agent思路是对齐的。

我反复让他练怎么讲清楚为什么要这么拆，每一层的边界在哪，层与层之间怎么通信，任务失败了怎么处理。刚开始他讲得磕磕巴巴，我让他写了逐字稿，练了不下十遍，最后终于能流畅地输出了。

还有一个点他做得挺扎实的，工具并发调度这块。多个Agent同时在执行任务的时候，怎么做任务队列管理，怎么避免重复执行，怎么处理超时，这些都做了。

面试官在技术面的时候问到了并发调度的实现，他给出的答案让对方觉得他真的想过这个问题，不是套方案。

还有一块，是很多人觉得不重要但其实很加分的，就是前后端打通的能力。

他项目里有一个地方用了SSE来做实时流式输出，就是AI在生成内容的时候，前端能看到字一个一个出来那种效果。

这个功能本身不算难，但他把FastAPI后端和TypeScript前端整个链路都自己跑通了，能讲清楚数据流的走向，能说清楚为什么不用WebSocket而用SSE。

面试官在系统设计那轮，正好问到了流式输出方案，他直接把项目里的实现细节说出来了，对方说了一句"你做过，我们不用讨论假设了"，然后直接进下一个问题。

这种感觉，跟背了答案然后背出来，真的不是一个体验。

聊完他的案例，想说一个更大的问题。

现在AI Agent岗位的竞争，已经不是两年前那种情况了。两年前你会用LangChain、搭个简单的RAG能问答，大家都觉得不错。

现在不行了。

进入这个岗位的人越来越多，大部分人手里都有几个项目，简历上都写了"基于LangChain实现了AI助手""搭建了RAG知识库"这类描述。

面试官看了一整天这样的简历，是麻木的。

区别你和别人的，不是你用了什么框架，是你能不能讲清楚你在系统里做了什么真实的决策。

为什么用这个存储方案而不是那个？

多Agent架构怎么做任务分配？任务失败了怎么回退？

并发场景下工具调用的副作用怎么控制？

这些问题，背八股文答不上来，只有真的做过的人才能答。

聊聊 他拿到offer那场面试。

技术面加上系统设计面聊了快两个小时，面试官主要围绕他的两个项目深挖，很多问题都是追问，追到很细节的地方。

他说当时有几个问题其实没答满意，但整体感觉还不错，因为他能清楚地说出哪些是他做了的，哪些是他了解但没深入的，没有乱说。

面试完两天出结果，过了。

事后他来跟我说，觉得自己答得有些问题不够好，为什么还能过。

我跟他讲，面试官在意的，从来不是你哪几道题答对了，而是这个人做没做过真实的事情，答不上来的时候有没有撑着胡说，还是老实说了我不确定。

真实感，比完美答案值 钱。

这个账号我一直在做的，就是帮大家搞清楚AI开发方向求职的真实情况，以及怎么把自己真实的经历包装成让面试官感兴趣的东西。

他不是天才，刚来找我的时候简历真的很弱。是我带着他一条一条梳理，项目怎么拆、架构怎么设计、量化怎么呈现、面试怎么讲，三个月的时间硬生生把他的项目经历从"跟着教程做的demo"拉到了"面试官愿意深挖的程度"。

很多人觉得自己背景不够好，学校不好、专业不对口、没有大厂实习。

我看了很多案例，这些其实都不是最关键的。最关键的是你有没有人帮你把真实的经历梳理成面试官能看懂的东西，然后你有没有能力把它讲清楚。

物理系的学生，在我辅导之后跑通了一套多智能体架构，拿下了大厂AI Agent岗的offer。

这件事本身，比我说任何话都有说服力。





前两周给一个学员看简历，他是做后端开发的，去年开始转AI方向，简历上密密麻麻列了一大堆东西。

Spring Boot、FastAPI、React、LangChain、RAG、Agent、Milvus、Redis、Docker，光技术栈就写了快两行。项目经历写了三个，每个项目四五行的样子。

我问他投了多少家了。

他说大概二十几个，接到面试的只有三个，而且全是一面挂。

我把简历翻到项目经历那页，手指着第一个项目让他自己念一遍。

他念完，我说好，你现在是面试官，你从这个项目描述里看出什么了。

他愣了一下，说，好像什么都看不出来。

对，问题就在这。

堆技术栈不叫AI全栈，把事情串起来才叫

AI全栈这个概念火起来之后，我看了很多人的简历，有一个特别普遍的现象。

很多人理解的AI全栈是这样，前端会一点，后端会一点，模型调用会一点，把这三块东西堆在一起，就是全栈了。

简历上就会变成这个样子。前端写了React和TypeScript，后端写了FastAPI和Python，AI那块写了LangChain和RAG。全部列完，觉得自己挺全面的。

但你去翻大厂的AI全栈JD，没有一条是要求你把三个方向的技术都罗列一遍。

阿里的AI全栈工程师JD写的是，负责AI产品的全流程开发，包括大模型Agent应用的架构设计、工程实现、系统部署及迭代优化。

看清楚没有，它要的是架构设计，是工程实现，是系统部署，是从头到尾把一件事情做完整的能力。不是你会多少个框架。

字节的AI研发岗JD也有类似的描述，要求独立完成从需求分析到系统上线的全流程。腾讯那边也是一样，招聘方要的不是一个什么都会一点的万金油，是一个能把模型能力真正接到业务里的人。

这才是AI全栈真正的含义。不是你的技术栈覆盖得多宽，是你的思维链路能不能从头串到尾。

面试官翻简历那15秒，他在找什么

我帮十几个学员复盘过面试，发现面试官翻简历的时候有个很有意思的规律。

他们看项目经历的注意力，平均也就15秒左右。

这15秒里他们在找什么呢，不是你会不会调API，也不是你用过多少工具。他们想找的是一个很具体的东西，你有没有把一个AI功能，从最开始的业务需求，到中间的技术选型和开发，到最终上线给用户用，这整条链路你搞没搞明白。

展开说，大概是这么几个点。

第一个，你做项目的时候做的那些技术决策有没有想清楚。比如你做RAG系统，为什么选了Milvus而不是Chroma，你的文档切分用的是固定长度还是按语义切，知识库更新了你用的是什么同步策略。这些不是框架帮你做的事，是你自己要想清楚的。

第二个，你的项目在实际运行中出过什么问题，你怎么解决的。面试官特别喜欢问这个。模型输出不稳定你怎么处理的，工具调用超时了你的系统怎么恢复，用户输入了敏感内容你的系统能不能兜住。这些问题Demo阶段一般不会遇到，真正上线了才会暴露。

第三个，你能不能把做的事情量化出来。不是说你要发明一个从没达到过的指标，而是你优化前后有没有对比。比如RAG检索的准确率从多少提到了多少，端到端延迟从多少秒降到了多少秒，系统的日均处理量从什么水平涨到了什么水平。有基线有结果，面试官才信。

同样一个项目，两种写法，差别有多大

我拿一个真实的例子来说。

一个学员的简历上写的是，负责智能客服Agent开发，基于LangChain实现多轮对话，调用GPT-4模型完成用户问题解答。

这句话语法上没什么问题，但它基本等于什么都没说。

多轮对话你是怎么设计的，用的是什么记忆机制，对话状态怎么管理的。工具调用失败了怎么办，有没有重试和降级。用户输入了敏感内容怎么处理。整个流程的端到端延迟是多少，有没有做过优化。

这些才是面试官看完之后想追问的东西。但你没有写在简历上，他就会现场提问，你大概率没准备过这些问题的答案，当场翻车。

同一个项目，换一种写法。

我负责智能客服Agent的架构设计和核心开发。对话记忆层用了滑动窗口加摘要压缩的方案，避免长对话context膨胀导致推理质量下降。工具调用链路加了超时重试和降级策略，外部API不可用时自动切到备用方案，保证核心问答不受影响。整条链路接了一层内容安全检测，敏感输入会走人工确认流程。埋了一套基础的观测方案，能追踪每一步的耗时和token消耗。上线后P99延迟从2.8秒降到0.9秒，日均处理咨询量从200条涨到1800条。

你看，同一个人，同一个项目，做的事情也差不多，但表述出来感觉完全不一样。

第一种写法等于把追问权交给了面试官，他问什么你不知道，答不上来就挂。

第二种写法你已经把面试官最可能追问的点提前写进去了。他会顺着你写的这些往下问，而这些点恰好是你最熟悉的。节奏在你手里。

写简历的时候有一个很实用的技巧，你写完之后自己对着项目描述问自己一串问题。这部分你当时怎么想的，踩过什么坑，优化前后差多少。如果你答不上来，就把那部分改一改。面试官跟你问的是同一个逻辑。

不同阶段的人，面试官看的重点不一样

说到这可能有人会想，我做的项目没这么复杂怎么办。

这个其实要看你目前在哪个阶段。面试官看不同阶段的简历，他的预期是不一样的。

刚入行或者经验比较浅的同学，真不用追求架构上的复杂度。面试官对你的预期不会太高，但你至少要做到一件事，在你负责的那个范围内，你能把来龙去脉讲清楚。

比如你在一个项目里负责了前端和AI接口的对接，那你能不能讲清楚长文本流式输出你是怎么处理的，前端怎么展示步骤化的Agent过程，中间的各种状态怎么管理。这些都是在你范围内可以深挖的东西，把它挖透了，面试官会觉得你做事很踏实。

有两三年经验的人，面试官的预期就不一样了。他希望你不仅能做事，还能说出你为什么要这么做。技术决策这块，你当时有哪些方案可以选，你为什么选了A而不是B，你选的时候考虑了性能和成本还是团队的技术积累。再比如你的RAG系统，为什么用了混合检索而不只是向量检索，你的chunk大小是怎么定的，踩过哪些坑之后调整的。能把这些讲清楚，面试官会觉得你有自己的判断力。

三年以上经验的，面试官看的角度又变了。他不太关心你会什么具体的技术，他关心的是你有没有独立从0到1把一个系统搭起来的能力，你对方案的可行性有没有自己的评估，你遇到问题的时候会不会自己拆解然后推进解决。这些东西不一定全写在简历上，但聊十分钟基本上就能判断出来。

2026年AI全栈的机会怎么样

说真的，今年AI全栈的招聘需求不差。

猎聘Q1的数据，AI应用开发相关的岗位比去年同期多了大概40%，薪资也在往上走。阿里的AI全栈岗30到50K乘16薪，字节的AI研发岗差不多也是这个水平，腾讯全栈测开方向今年岗位需求涨了340%。

但门槛确实比去年高了。去年可能你学过LangChain、做过一个RAG Demo就能约到面试，今年不行。企业招人的思路变了，是真金白银要出产品的，不是拿来做实验的。他们要的是能把模型能力真正接入业务的人，不是只会调API的人。

对认真准备的人来说这是个好事情。门槛筛掉了一批人，机会反而更集中了。

你不用什么都会。你只需要在AI全栈这条链路上，比你同阶段的竞争者多往下挖一层。面试官翻你简历那15秒，能不能看到一个能独立把事情做完整的人，这才是拉开差距的地方。





上个月帮一个学员复盘面试，他说面AI Agent方向的时候，面试官让他介绍项目。

他讲得很兴奋，说自己接入了MCP，做了RAG检索，还实现了Function calling让大模型能调用外部工具。讲完之后他觉得自己表现不错。

结果面试官说了一句：你这个太toy了。

他当时没太理解，问我toy是什么意思。我说就是玩具，意思是你这个项目只能跑通Demo，离工程化还差得远。

这句话很扎心，但确实是现在AI Agent面试的一个普遍现象。很多人花了一周搭了个MCP加RAG加Function calling的组合，觉得这就是Agent了。但面试官看的不是你会不会搭积木，而是你有没有把一个Agent系统做到能上线、能持续迭代、能处理真实业务复杂度的水平。

最近我在小红书上看到一个帖子，讲Agent项目太toy了怎么改，列出了七个升级方向。我把这些方向重新梳理了一下，结合我看到的真实面试案例，说说怎么让你的Agent项目从玩具变成能写在简历上的硬货。

MCP加RAG加Function calling，真的只是起点

先说清楚，MCP、RAG、Function calling这三个东西，是Agent的底层组件，但把它们拼在一起不等于一个完整的Agent系统。

打个比方，你买了CPU、内存、硬盘，把它们插在主板上，这不叫电脑。电脑需要操作系统来调度资源、需要驱动来连接硬件、需要散热系统来保证稳定运行。Agent也是一样，MCP是接口协议、RAG是知识检索、Function calling是工具调用，但把它们串起来的推理框架、记忆管理、多Agent协作、成本优化，才是决定这个项目能不能上线的关键。

面试官说太toy了，通常不是嫌你技术栈不够新，而是嫌你的项目缺少工程化思维，没有处理过真实场景的复杂度、没有考虑过系统的可扩展性、没有做过性能优化。

升级方向一：让Agent不只是读文字

很多入门级的Agent项目，用户输入只能是文字。但真实业务里，用户上传一张图、发一段语音、传一个视频，都是常见需求。

升级方向是引入多模态大模型推理。文字、图片、语音、视频，这些不同模态的输入，通过多模态模型统一理解，再转化成Agent可以处理的标准格式。这个实现起来并没有想象中复杂，现在很多大模型已经原生支持多模态输入，关键是你的Agent架构能不能兼容不同模态的数据流。

写在简历上，你可以说自己设计了一套多模态输入处理管线，支持文字、图片、语音的统一接入和标准化解析。这比单纯说接了MCP要高级得多。

升级方向二：让Agent自己决定要不要查资料

很多toy项目的流程是固定的：用户提问，查RAG，调Function，返回答案。这个流程写死了，不管用户问什么，都要走一遍完整的检索和调用。

更好的做法是引入ReAct框架，让Agent自己判断：这个问题需不需要查知识库？需不需要调用工具？还是直接靠大模型本身的推理能力就能回答？

这个自主决策能力，是区分Demo和产品的关键。Demo的流程是写死的，产品的流程是动态的。Agent要根据问题的类型、上下文的复杂度、知识库的覆盖范围，实时决定最优的处理路径。

升级方向三：RAG别只会暴力检索

普通RAG的做法是：用户提问，向量化，去向量库里搜最相似的片段，塞进Prompt，大模型生成答案。

这个做法有两个明显的问题。第一个问题是每次都查，不管这个问题需不需要查。第二个问题是查出来的片段质量不稳定，有时候相关性不高，有时候片段之间缺少关联。

升级的方向有几个。agentic RAG是让Agent自己决定检索策略，不是每次固定检索。知识图谱增强RAG是把结构化知识引入进来，解决片段之间缺少关联的问题。动态路由RAG是根据问题的类型，自动选择不同的检索路径。

这些升级的核心目的，是让检索变得更聪明，而不是更暴力。

升级方向四：检索策略交给LLM自己决定

这个是升级方向三的延伸，但值得单独拿出来说。

自适应RAG的思路是，LLM自己判断：这个问题需不需要检索？如果需要，检索哪个知识库？检索哪些片段？要不要多轮补充检索？

这样一来，你的Agent系统里就需要有一个小型的推理层，专门负责制定检索策略。这个推理层可以是一个轻量级的LLM调用，也可以是一个规则引擎，但核心是把这个决策从写死的逻辑里解放出来。

面试官很喜欢问这个点，因为它考察的是你对RAG系统的深度理解，不是会搭向量库就行，而是能优化检索质量和效率。

升级方向五：给Agent装上一个长期记忆

toy项目的Agent是没有记忆的，或者只有当前对话的短期记忆。每次对话结束，上下文就丢了，下次再来用户得重新说一遍。

工程化的Agent需要长期记忆。至少有两个层面的记忆：一个是对话摘要，把历史对话压缩成关键信息，下次直接加载。另一个是用户画像记忆，记录用户的偏好、习惯、常用需求，实现个性化的服务。

实现上可以用memory agent专门负责记忆管理，定期对历史对话做摘要和向量化存储。这样Agent就能做到越用越懂你，而不是每次从零开始。

升级方向六：从一个人干活到团队协作

单Agent的能力是有上限的。复杂任务往往需要多个Agent协作，每个Agent负责不同的角色。

比如一个电商客服场景，可以拆成三个Agent：意图识别Agent负责理解用户想干什么，知识检索Agent负责查商品信息和政策，回复生成Agent负责组织语言和生成回答。三个Agent之间通过CoT分工协作，而不是一个Agent包揽所有事情。

多Agent的调度是一个很有意思的工程问题。怎么分配任务、怎么同步状态、怎么处理冲突，这些都是面试官喜欢追问的点。

升级方向七：通用模型不够用时，自己微调

前面六个方向都是架构和工程层面的优化，但有时候瓶颈在模型本身。

通用大模型在某些专业领域的表现不够好，或者推理成本太高。这时候可以考虑用LoRA微调，在通用模型的基础上训练一个专用的小模型。

但这里有一个坑要注意：微调用的数据集和你RAG知识库里的数据不能重合。如果知识库里的内容都被模型记住了，那RAG的价值就被削弱了，而且模型可能会过拟合。正确的做法是把数据分成微调训练集、验证集和RAG知识库，三者互不重叠。

这个点面试官很爱问，因为它考察的是你对模型和数据关系的理解深度。

写在简历上，怎么让这个项目变高级

说完七个升级方向，说点实际的。如果你正在准备AI Agent方向的简历，项目描述不要写实现了MCP、RAG、Function calling的Agent系统，这个表述一看就是入门级。

更好的写法是突出工程化和复杂度。比如：

设计并实现了多Agent协作的客服系统，包含意图识别、知识检索、回复生成三个Agent模块，通过SpringAI实现调度与状态同步。

实现了自适应RAG检索策略，LLM根据问题类型动态决策检索路径，检索准确率从基线的65%提升到82%。

引入了长期记忆机制，对话摘要与用户画像双轨存储，用户重复提问率下降40%。

你看这些描述和接入了MCP和RAG之间的差距。前者在说功能，后者在说价值。

最后说几句

AI Agent这个方向现在确实热，但热的同时也在快速分层。会搭MCP加RAG的人已经满大街都是了，能把Agent做到工程化级别的人还是少数。

这个分层对正在找工作的人来说，既是挑战也是机会。挑战是你的项目得真的过硬，不能只是Demo。机会是只要你的项目比其他人的更专业一点，面试的时候就能明显被区分开。

如果你的Agent项目还停留在toy阶段，试着从上面七个方向里挑两三个，动手升级一下。这个过程本身就能学到很多东西，而且升级后的项目写在简历上，完全是另外一个级别。






一个学员 上周面字节，回来跟我吐槽。

他说自己准备了三个月，HashMap底层、JVM垃圾回收、MySQL索引结构，背得滚瓜烂熟。结果面试官开口第一题不是HashMap原理是什么，而是你们系统的QPS峰值是多少，如果突然涨十倍，你的服务会挂在哪里。

他愣了五秒，说了一句很诚 实的话：这个我没想过。

面试挂在一面。

这不是个例。我最近跟十几个正在找工作的后端聊下来，发现一个很明显的趋势：2026年的后端面试，问法已经彻底变了。

八股文没用 不是因为你背 得不够

先说清楚，八股文不是没用，是问法变了。

以前面试官问HashMap，你背一通数组加链表、红黑树、负载因子0.75，这关就过了。现在面试官问HashMap，他的问题长这样 ：并发下有什么问题？容量为什么是2的幂？扰动函数为什么右移16位？树化阈值为什么是8？

你看出来区别了吗。

他不是在考你知道HashMap有什么，而是在考你碰到具体问题的时候，能不能做出判断。前者是记忆题，后者是判断力题。

我举一个更典型的。线程池，以前背corePoolSize核心线程数、maximumPoolSize最大线程数就完事。现在面试官会问：你们业务的线程池，IO密集型和CPU密集型分别怎么设参数？拒绝策略你选的哪个？为什么？

注意这个 为什么。

这才是整套题 的核心。你选CallerRunsPolicy还是AbortPolicy，本身没有对错。但你必须能说清楚，在你的业务场景里，为什么这个策略比其他更合适。你的回答里得有场景、有取舍、有后果推演。

这才是2026年面试官真正 在听 的。

场景题到底在考 什么吗

我总结了一下，现在后端面试高频场景题，面试官其实在同时测你几个东西，我挑三个最关键的说。

源码理解度排第一个。

不是让你背源码全文，那太蠢了。面试官只看关键路径上的关键设计。HashMap的put方法，你不用每一行都懂，但你得知道什么时候会扩容、扩容时旧数据怎么迁移、为什么迁移时其他线程可以帮忙。这些都是关键路径，因为它们直接决定了并发场景下的行为。

第二个东西，线上问题定位能力，这部分是最能拉开差距的。面试官会问你：Redis突然变慢了，你怎么排查？注意，他不是在考你知道不知道latency doctor这个命令，他是在考你按过哪些命令、看过哪些指标、怎么一步步缩小范围。

高分回答长这样：先看latency doctor确认是不是Redis本身慢，再用slowlog看有没有慢查询，然后用bigkeys扫描大key，同时用monitor看实时流量。每一步都有原因，每一步都能解释为什么先看这个、后看那个。

低分回答就是列命令名，latency、slowlog、bigkeys一顿堆，听起来像在背运维手册。

还有第三个，设计取舍能力， 也就是Trade-off，这是最难的部分。面试官会给你一个开放问题，比如设计一个短链系统，然后不断加条件逼你改方案。一开始你说用自 增ID加base62编码，他说每天生成量上亿你这方案行不行。你说那换分布式的，他用单点故障再压你一 遍。

你不需要给出完美方案，那不可能。你需要做的是，每轮都能说清楚：当前方案在什么条件下成立、有什么缺点、换这个方案的代价是什么、我为什么选它。

这个能力的核心是判断力，不是知识量。

拿线程池举个完整例子

还有第三个，设计取舍能力， 也就是Trade-off，这是最难的部分。面试官会给你一个开放问题，比如设计一个短链系统，然后不断加条件逼你改方案。一开始你说用自增ID加base62编码，他说每天生成量上亿你这方案行不行。你说那换分布式的，他用单点故障再压你一 遍。

先说业务场景，他们是图片处理 服务，单次请求IO等待占大部分时间，所以线程数可以设多一些。然后说具体参数，corePoolSize设了40，依据是压测时这个数值CPU利用率最平稳。拒绝策略选CallerRunsPolicy，理由是这个服务延迟敏感，拒绝不如让调用方自己跑来得快，至少能让上游感知到压力起到反压效果。最后他说还加了监控，活跃线程数、队列长度、拒绝次数三个指标全打到了Grafana大盘上。

面试官听完没追问，直接过了。

反过来，我听过太多低分版本了：corePoolSize是核心线程数，maximumPoolSize是最大线程数，CPU密集型设CPU核心数加一，IO密集型设两倍。

背完了。面试官问：为什么加一？你这个两倍是怎么算出来的？你的业务 图片处理，IO等待时间平均多长，你测 过吗？

源码只看关 键类，别贪多。HashMap、ConcurrentHashMap、ThreadPoolExecutor、ReentrantLock/AQS，这四个类的关键路径画一遍调用链流程图，比背十个源码全文都有用。面试官追问的时候，你能说清楚这个方法在这里调用了那个方法、原因是xxx，这就够了。

这两个回答的本质区别，低分在背教科书，高分在讲我遇到过这个场景、我做了选择、我知道选这个的后果。面试官要的就是这个。

2026年，后端面试到底怎么准备

说完变化，说点 实际的。如果你最近正在准备后端面试，我建议你把时间花在几个地方，比刷一百道八股文有用得多。

源码只看关 键类，别贪多。HashMap、ConcurrentHashMap、ThreadPoolExecutor、ReentrantLock/AQS，这四个类的关键路径画一遍调用链流程图，比 背十个源码全文都有用。面试官追问的时候，你能说清楚这个方法在这里调用了那个方法、原因是xxx，这就够了。

准备线上救火故事，每个工具至少一个。MySQL、Redis、JVM，各准备一个你真实排查过的问题。故事的结构要完整：先说现象（CPU飙到90%），再说你按了哪些命令（top、jstack、arthas），再说你看到了什么指标（有个线程占了60%CPU），最后说你怎么解决的（改了一段代码，CPU降下来了）。

没有真实经历怎么办。那你把常见的排查案例理解透，转化成我见过类似问题的表述，别硬编。

深度推演两个系统设计题，不用多。推荐短链系统加排行榜系统，这两个是高频中的高频。每个都要能说出三种存储选型的对比，比如排行榜用Redis Sorted Set vs MySQL vs 分层缓存，各自的优缺点、适用场景、数据规模边界在哪。

面试官问系统设计，他不是在考你知道不知道某个技术，他是在考你面对一个模糊问题时，怎么拆解、怎么权衡、怎么把一个大致方向推演成可落地的方案。这个能力，刷LeetCode刷不出来。

最后说几句

2026年的后端求职市场，我观察下来，机会其实不少。云 计算、微服务、AI基础设施这一波，对后端工程师的需求是在涨的，不是缩的。

但面试的门槛确实高了。以前你背好八股文就能过中小厂，现在不行了，面试官问的都是你有没有真刀真枪干过活。这个变化对只会背书的人很不友好，但对有真实项目经验的人来说，其实是好事，因为现在更容易区分谁真的能干活，谁只是在准备面试。

如果你觉得自己项目经验不够扎实，或者面试总是差一口气过不了，这件事是可以系统地准备的。不是多刷面试题，而是把你的项目重新梳理一遍，把每个技术选型的理由、每个线上问题的排查过程、每个性能优化的前后对比，都整理成能讲清楚的故事。

这件事做好了，面试官问什么你都能接住。





有个做了两年后端的朋友，年初转了大模型应用开发方向，简历上写了三个RAG项目，信心满满地去面了一家中型AI公司。

技术面聊了半小时，对方问了一句，你们的RAG怎么做的？

他说，用了LangChain，接了Milvus做向量检索，调了OpenAI的接口生成回答。

面试官点了点头，又问了三个问题，他一个都没答上来。

第一个，向量检索用的什么相似度？为什么不用欧氏距离？

第二个，chunk size设多少？切大了和切小了分别有什么问题？

第三个，检索到了正确内容，模型还是编造了信息，你怎么处理的？

这三个问题，没有一个超纲，没有一个需要你读过什么论文，但全部指向同一个问题，你以为你做了RAG，其实你只是跑通了别人的pipeline。

RAG面试，面试官到底在考什么

很多人对RAG的理解停留在接接口调包的层面。装一个LangChain，连一个向量数据库，把文档切片丢进去，调GPT生成答案。这个流程确实能跑通demo，但面试官问的不是你会不会调包。

RAG面试考的，是你有没有真正踩过坑，有没有理解每个环节为什么这么做。

说真的，2026年的大模型面试，RAG几乎成了必考题。不管你投的是大模型应用开发、AI后端还是LLM工程师，面试官大概率会围绕RAG展开追问。真正高频考的，不是Transformer架构，不是注意力机制，而是那些你在做RAG项目时会碰到的真实问题。

五个最容易暴露你只是调包的问题

第一，检索策略

面试官问，你们用纯向量检索还是混合检索？

很多人的回答是，用了向量检索，效果还行。

但面试官真正想听的是，你知不知道纯向量检索在精确匹配上的短板。搜一个具体的产品编号、一个人名、一个缩写，纯向量检索经常召回的不是最精确的那条，而是语义上比较接近的。搜RFC 7231，可能返回的是一篇讲HTTP协议规范的文档，而不是RFC 7231本身。

所以现在比较成熟的方案是混合检索，向量检索加BM25关键词检索，再用RRF公式合并排序。RRF的好处是比固定权重更稳健，因为不同问题的最佳权重差异很大，你固定一个权重不可能适配所有场景。

如果你能说出这些，面试官就知道你真的做过。

第二，Rerank重排序

面试官问，混合检索之后为什么还要Rerank？

这个问题的答错率非常高。很多人会说，让结果更准。但说不清为什么更准。

关键区别在于编码方式。第一阶段的检索是Bi-Encoder，问题和文档分别编码算相似度，快但粗糙，只能算个大概相关。Rerank是Cross-Encoder，把问题和文档拼在一起送进模型，模型同时看到两边的内容，判断精准度高很多。

代价是速度慢，所以不能对所有文档都做Rerank，一般是先检索Top20粗筛，再Rerank精排取Top5。实测下来，Top5召回率能从71%拉到89%，这个提升非常明显。

第三，文档切分

面试官问，chunk size设多少？为什么？

这也是一道判断你有没有真做过的题。如果只说设了512，基本就暴露了。

chunk size是一个需要权衡的参数。切太大了，信息被稀释，真正相关的内容淹没在大量无关内容里，相似度分数反而会降。切太小了，上下文断裂，模型看到的是断章取义的碎片，根本理解不了完整含义。

生产环境最常用的是递归切分，按段落、句子、字符的优先级递归拆分。还有一个很多人忽略的点，overlap，相邻chunk之间重叠10%到20%，防止关键信息正好落在切割点上。这个点很多人没提过，面试时一说出来，差距就拉开了。

第四，幻觉处理

这个问题几乎必问。面试官会说，检索到了正确内容，模型还是编造了信息，你怎么办？

只说用了Prompt约束是不够的。面试官想听到的是一个策略组合。

比较完整的做法至少包括三个层面。Prompt层面，明确要求只基于检索结果回答，没有就说不知道。生成层面，用输出自校验，生成后再过一遍模型检查每条信息是否有依据。参数层面，把temperature调低到0.1到0.3之间，降低随机性。

如果你还能提到兜底机制，比如检索结果和回答做相似度对比，低于阈值直接返回未找到相关信息，那就更好了。

我见过一个回答特别漂亮的，说他们把幻觉率从30%降到了12%，靠的就是Prompt约束加输出自校验加温度调低的组合。有具体数字，说服力直接翻倍。

第五，Agentic RAG

这是2026年比较新的考点。面试官问，了解Agentic RAG吗？

普通RAG的检索是固定的，检索一次就生成。Agentic RAG让模型自己决定要不要再检索，不满意就换策略重搜，复杂问题可以拆成子问题分步查。

但这里有个关键点，面试官想听的不是你把它吹得多厉害，而是你有没有工程判断力。Agentic RAG适合复杂场景，比如法律、医疗、金融。简单问答用它就是过度设计，Token消耗高，延迟大。

能说出这句话，比背十个Agentic RAG的定义都有用。

这些问题的本质是什么

仔细想想，上面这些问题的共同点是什么。

不是考你记住了多少概念，而是考你有没有在真实项目里做过决策。

chunk size设多少不是一个标准答案，是一个工程权衡。用不用Rerank不是一个对错题，是一个成本和效果的取舍。幻觉处理方案选哪个，取决于你的业务对准确率的要求有多高。

面试官真正想找到的，是那个在这些决策面前有过思考、有过实践、有过优化的人。

而不是那个只会在简历上写实现了基于RAG的智能问答系统的人。

怎么准备才有用

如果你手上已经有一个RAG项目，不要只停留在跑通的阶段。回去重新审视每一个参数的选择，搞清楚为什么是这个值而不是那个值。把检索延迟测一测，把幻觉率统计一下，尝试用混合检索替换纯向量检索看看效果差多少。

这些真实数据，面试的时候比任何标准答案都有说服力。

如果你还没有RAG项目，现在开始做也不晚。不用做得多复杂，一个能跑通的RAG问答系统，加上混合检索和Rerank，再花时间调优chunk参数和处理幻觉问题，就已经超过大部分人了。

2026年大模型应用方向的机会其实很集中。不是所有人都在卷算法，大部分企业在找的是能把RAG、Agent这些技术真正落地的人。面试官反复追问这些细节，不是在为难你，是在确认你能不能把事情做成。



昨天一个我带过的学员给 我发了张截图。

点开一看，阿里Offer。 我屏幕看了好几秒。不是因为阿里有多稀罕，是我太清楚他这几个月是怎么熬过来的。

211本，QS前50硕，科班出身。这个背景放在市场上算能打的。

但他前前后后投了20多场。

腾讯，2面挂。快手，3面挂 。中间还有一些公司，一面之后就没下文了。最崩溃的那段时间他跟我语音，语气低到我差点没听清他说话，问我是不是方向选错了，是不是自己不适合搞技术。

我跟他说，你这不叫不适合。你只是不知道怎么面试。

纯Java选手转AI，代码能力没问题，面试完全不行

他原本就是扎扎实实的Java开发路线。Spring全家桶、JVM调优、分布式、微服务，常规面试题他其实都能聊。

但今年行情变了。

传统Java后端的岗位增速在放缓，AI应用开发的岗位在猛涨。他自己也感觉到了，决定往AI方向转。

转型本身没有想象中那么难。他底子好，大模型应用 、Agent、RAG这些方向一两个月就上手了。问题不在会不会。

问题在面试。

他自己后来复盘，之前挂掉的面试有一个共同规律。项目经历讲不出来。

不是没做东西，是讲的方式完全不对。面试官问到项目他就开始堆技术名词，问到第二层怎么做的决策就答不上来。到了二三面的场景题环节，基本是问一个倒一个。

这不是能力问题。他知道自己做了什么，但他不知道面试官想听什么。

三个星期，我们只做了三件事

我带他走的流程其实不复杂。

第一件，拆项目。

不是简历上堆技术栈那种写法。是把每个项目拆成三个关键维度。你做这个东西解决了什么业务问题，技术上碰到了什么硬骨头，你是怎么做的技术决策和取舍。

面试官看项目经历的时候，最想知道的不是你会用什么工具。是你做决策的逻辑是什么。技术选型的时候为什么选A不选B，方案设计的时候怎么在成本和效果之间做平衡。

第二件，场景题。

AI方向这两年面试有一个明显变化。不太考LeetCode和传统八股了，而是直接甩你一个真实业务场景。比如线上服务QPS突然翻了三倍你怎么排查，比如从零设计一个RAG系统你打算怎么分层。这些题没有标准答案。

我带他练了十几道场景题。不让他背答案，是教他怎么拆一个陌生问题。怎么快速理出思路框架，怎么把自己的知识储备串成完整的逻辑链，怎么被追问到第三层第四层的时候还能跟住。

第三件，模拟面试。五六轮，全真模拟，每一轮录音复盘。

第一轮录完他自己都听不下去 ，问我怎么讲成这个鬼样子。到第三轮的时候状态明显不一样了，语气稳了，不抢答了，知道稍微停一下再组织语言。

四月到五月，全都扛住 了

4月24号一面。

5月7号二面。

5月8号三面。

5月12号HR面。

二面和三面全是场景题深挖。但这次他扛住了。不是押中了原题，是他学会了在陌生题目上快速拆解和推导。面试官层层追问的时候，他能跟住逻辑，不掉链子。

HR面当场口头Offer，没有横向比较，直接定了。AI应用研发工程师，暑期实习岗。

他说拿到Offer的那一刻，第一反应不是激动，是脑子 里闪过这几个月被拒了一次又一次的画面。然后才觉得，值了。


求职这件方法很重要

我做了这么久求职辅导，发现一件事。

大部分人面试挂掉，跟技术能力没有直接关系。

是信息差。

你不知道面试官看到你简历的那一刻判断标准是什么。你不知道怎么把做过的项目讲出含金量。你不知道那些挂在二面三面的人，卡的点跟你几乎一模一样。

这些东西靠自己摸，要投几十场面试可能才摸到一点门。但跟一个在这个行业干了很多年的人聊一两个小时，一个下午就够了。

上面说的这个学员，从前前后后挂掉20多场面试，到拿到阿里Offer，前后一个多月。代码能力没变，技术栈没变，变的只是面试方法。

如果你现在也在找工作

AI方向机会确实多，但挤进来的人也越来越多。

简历上大家写的东西都差不多。大模型、LangChain、RAG、Agent，这些关键词谁都会放。那你凭什么让面试官在几十份差不多的简历里挑中你，凭什么一个小时的面试结束之后对面那个人觉得这个人我一定要。

靠的不是你会多少框架，是你能不能把自己的经历讲明白、讲 的透。


最近帮一个学员改简历，他之前在一家互联网公司做后端开发，去年转去做AI方向。简历上写了一个Agent项目，描述是这样的：参与构建智能代码助手，使用大语言模型提升开发效率，支持代码补全、错误诊断等功能。

我说你这写得也太淡了。他说那要怎么写？我说你先告诉我，你在这个项目里到底做了什么？

他愣了一下，说就是调了OpenAI的接口，写了一些prompt，让模型能根据代码上下文给出建议。

这就是问题所在。不是他做得不够，是他不知道怎么把自己的工作写出价值感。

为什么你的Agent项目看起来很水

现在不管是校招还是社招，简历上没个Agent项目都不好意思投AI相关的岗位。但问题是，大部分人写的Agent项目都长一个样：使用LangChain搭建智能体，集成GPT-4实现某某功能，提升了XX效率。

面试官一看就知道，这人大概率就是调了个API，套了个框架，没有自己的设计思考。

真正有价值的Agent项目，核心不在于你用了什么模型，而在于你解决了什么问题，以及你是怎么让模型和工具协同工作的。面试官想考察的是：你理解Agent的决策逻辑吗？你设计过工具调用流程吗？你有没有考虑过上下文管理、状态恢复、运行安全这些工程问题？

说得直白点，面试官要的不是一个会写prompt的人，而是一个能设计系统的人。

一个学员的真实案例

我带的这个学员，之前在一个团队里负责内部AI研发工具的搭建。他们当时面临的问题很典型：用大模型做代码分析、重构、审计这类长链路任务，跑几轮之后prompt就膨胀得离谱，同一个文件反复读，中间执行断了还得从头来，工具调用也经常出副作用，最后产出的结果根本没法复盘。

他没选择走捷径，而是从头设计了一套本地化的智能体执行框架。项目名字叫DevPilot，核心用的Python，技术栈包括Agent Orchestrator、Tool Invocation、Context Budgeting、Checkpoint & Resume、Tiered Memory、Execution Trace。

下面是他简历上这个项目的完整写法，我直接贴出来给你们参考：




好项目好在哪

你可能会问，这个项目和其他Agent项目有什么区别？区别就在于深度。

大多数人写Agent项目，只写到了功能层面：我做了什么功能，用了什么技术。但这个项目写到了设计层面：我为什么要这么设计，我是怎么解决工程难题的，我的方案带来了什么量化效果。

比如上下文压缩这个事，不是简单说一句我做了prompt优化就完了。他写清楚了：在什么场景下做的、压缩率是多少、最高能压多少、怎么保证不丢语义。这就是面试官想看到的工程思维。

再比如断点续作，不是简单说支持了任务恢复。他写清楚了：覆盖了多少场景、识别率是多少、有没有出现过误判。这就是可验证的工程能力。

Agent项目简历的核心写法

如果你也要在简历上写Agent项目，记住这几个原则：

第一，不要堆技术名词。 写了LangChain、LlamaIndex、ReAct这些框架名，不代表你懂Agent。重点是讲清楚你让模型在复杂场景下，能自己判断该用哪个工具、怎么用、什么时候停。

第二，要有量化数据。 压缩率百分之十六、重复读文件从六十次降到零、识别率百分之百，这些数字比任何形容词都有说服力。

第三，要体现设计思考。 不要只写做了什么，要写为什么这么做。比如为什么选择分层缓存而不是全局缓存？为什么用checkpoint而不是重放整个对话？这些设计取舍才是体现你技术深度的关键。

第四，要覆盖完整链路。 一个好的Agent项目，至少应该包括工具定义、控制策略、决策逻辑、监控反馈这几个维度。只写其中一两个，面试官会觉得你思考不全面。

给想转AI方向同学的建议

现在AI方向的机会确实很多，但竞争也很激烈。如果你只是跟着教程跑了一个Demo，然后往简历上一放，面试的时候很容易被问住。

真正有价值的是那些你从0到1设计过、从1到10优化过的项目。哪怕项目不大，只要你有自己的设计思考，能讲清楚每个技术决策背后的原因，就已经超过了大多数人。

如果你现在还没有这样的项目，可以从一个具体的问题场景入手。比如你们团队日常开发里有什么重复性工作可以用Agent来优化？不要贪大，先把一个小场景做深，做出可量化的效果，这比做一个大而空的平台要有价值得多。




上个月带一个学员准备Agent方向的面试，做了一件很聪明的事，对。

不是刷了多少道题，而是把腾讯云那套Agent Memory的上下文压缩方案，自己动手做了一遍。从Mermaid无限画布到上下文卸载，从四级记忆分层到消融实验，完整跑通。

结果面试某大厂AI 平台部时，面试官连续追问了20分钟这个项目。最后给了ssp。

这件事让我意识到一 Agent岗面试，已经从考概念进化到考工程落地能力。你能讲清楚trade-off不算厉害，你能拿出一个可量化的优化方案才算真本事。

一、Agent Memory为什么是面试 的深度分水岭


现在的Agent面试，记忆管理几乎是必考题。但大多数人的回答停留在教科书层面：短期记忆用滑动窗口，长期记忆用向量数据库，检索时做相似度匹配 。

这种回答不会扣分，但也绝不会加分。因为面试官听过太多次了。

真正能让面试官抬头的，是你能讲出一个具体的优化案例：上下文窗口是怎么被撑爆的，你是怎么做压缩的，最终省了多少token，任务通过率提升了多少。

有数据支撑的项目经验，和背概念的回答，差距大概是50万年薪 。

二、腾讯云方案 的精髓：不是删内容，而是分层折叠
腾讯云那套Agent Memory方案的核心思想，我觉得可以用一句话概括：压缩不是让Agent少知道，而是让Agent少背负。

具体来说的 话，他们把信息分成四层来处理。

第一层是原始证据。 工具返回的完整结果、网页内容、文件读取记录，全部存到外部文件系统。上下文里只留一个文件路径。

第二层是工具调用摘要。 每次工具调用结束后，生成一条JSONL记录，包含时间戳、节点ID、操作摘要和结果引用。这样Agent不需要读完整文件，先看摘要就能判断有没有必要下钻。

第三层是任务画布。 用Mermaid图语言把任务执行过程可视化，每个节点包含状态、摘要和时间戳。Agent一眼就能看出任务进行到哪一步，哪些分支已经完成，哪些还在进行。

第四层是任务元信息。 当上下文压力继续升高时，只保留最轻量的metadata：任务目标、状态、节点数量、画布文件路径。需要恢复时再按路径找回。

这套分层的妙处在于，信息可以根据需要逐级展 开。平时Agent只看画布节点，需要细节时查JSONL，还不够再读原始文件。既省了token，又没有丢失信息的可达性。

三、我们是怎么落 地的：从论文到代码
学员在复现这套方案时，我 让他重点做了三件事。

做量化对比

不 说省了多少token，要有对照实验。设计了一个 超长Session测试：把50个SWEbench任务串在同一个session里连续执行，对比有压缩和无压缩的差异。

结果是：token消耗最高省了61 %，任务通过率从33%提升到50%。

这组数据放在简历里，比写十句自我评价都有说服力。

讲清楚trade-off。

面试官一定会问：压缩会不会丢失信息？什么时候该压缩，什么时候该保留？

回答是：压缩的前提是可恢复。原文存在refs目录，摘要存在JSONL，画布存在MMD文件，metadata留在上下文。四级索引保证了任何一层信息都能被找回。但如果Offload模型质量不够，摘要生成不准确，确实会导致信息损失。所以方案里用了多个Offload模型做对比，最终选效果最好的那个。

这种回答体现的是工程思维，是不背答案 的。

结合实际场景讲价值。

举了一个例子：在编程任务里，Agent会不断读取文件、运行测试、查看报错。如果没有压缩，上下文很快会被旧日志和无关代码片段填满，模型后半程根本找不到关键信息。但有了分层压缩，Agent可以保留bug定位路径和关键文件摘要，后半程仍然能聚焦在核心问题上。

面试官听到这里，基本就确定这人是真做过事的 。

这个项目 怎么写到简历里
很多人有个误区：觉得项目经验写得越全越好，把用了什么技术栈全部列上去。

实际上的 ，简历里的项目描述，核心就三个数字：问题、方案、结果。

以这个Agent Memory项目为例，我建议 他这样写：

设计并实现Agent短期记忆压缩方案，解决长任务场景下上下文窗口膨胀导致的token浪费和任务失败问题。采用上下文卸载加Mermaid无限画布的分层架构，将信息分为原始证据、工具摘要、任务画布、任务元数据四级存储。在SWEbench超长Session测试中，token消耗降低61%，任务通过率从33%提升至50%。

三段话，说清楚做了什么、怎么做的、效果如何。

注意，这里没有罗列技术名词，而是讲了一个完整的故事：遇到了什么问题，用了什么思路，拿到了什么结果。

面试 会怎么追问，你怎么 说呢
基于这个项目，我帮他模拟了几轮追 问。

一：为什么选择Mermaid而不是其他图语言？

标准回答：Mermaid是通用格式，所有主流大模型都能读写；语法简单，生成和理解的认知负担一致；拓扑自由度高，支持任意分支、合并、循环。实验中对比了Flowchart和StateDiagram，Flowchart效果提升约15%，因为Agent的执行过程不是严格状态机，而是开放空间的探索式过程。

追问二：如果Offload模型生成的摘要不准确怎么办？

标准回答：这是方案的关键风险点。我们的做法是保留原始证据的完整路径，摘要只是索引。当Agent发现节点摘要不够支持下一步判断时，可以通过node_id追溯到JSONL记录，再通过result_ref读取完整原文。摘要不准确最多影响效率，不会导致信息永久丢失。

三 这个方案和直接用RAG有什么区别？

标准回答：RAG解决的是知识检索问题，把外部知识注入上下文。我们解决的是任务状态管理问题，把Agent自己的执行历史组织成可恢复的结构。两者可以共存：RAG注入领域知识，Memory管理任务状态。

这三轮追问答下来，面试官对候选人的评价通常是：有工程意识，有数据思维，有边界认知。

腾讯云那套Agent Memory方案之所以值得复现，不只是因为技术本身先进，更因为它提供了一个完整的工程案例：有架构设计、有量化实验、有消融对比、有落地场景。

对于正在准备Agent方向面试的开发者，我的建议是：找一个类似的开源方案，自己动手做一遍，跑出数据，然后把它变成你简历上最亮眼的项目。比 刷100道面试 题都管用。

如果你=正在准备Agent方向的面试，或者想把类似的项目经验打磨成面试官无法拒绝的简历，




前两天 跟一个在阿里 做AI平台的朋友吃饭，。

他们组 最近在招Agent方向的开发，面了将近20个人，最后只发了3个offer。不是候选人不够聪明，而是大多数人搞错了一件事：把Agent开发当成了纯算法岗在准备 的 这部太好

实际上，企业招Agent开发，要的是能把大模型从玩d具变成生产力工具的人。原本装在个人电脑上的助手，改造成面向企业的服务，加上权限控制、环境隔离、记忆隔离、容器化部署。核心逻辑就一个：让AI真正能在企业场景里跑起来，而不是停留在demo阶段。

这个方向，恰恰是2026年最缺人的赛道。

Agent面试，到底在考什么
我整理了最近字节、腾讯、快手、Shopee等厂的Agent面试真题，发现考点可以归为三类。

第一类，基础概念辨析。

比如，面试官会问：ChatBot加上s插件，算不算Agent？RAG加Chat，又算不算？

很多人背了一堆定义，但答不到点上。判断标准其实就一个：有没有多步自主决策和反馈闭环。单次检索增强只是更聪明的ChatBot，只有能根据环境反馈不断调整行动的，才叫Agent。

第二类，架构设计能力。

这是拉开差距的核心战场。一d道高频题：设计一个企业智能客服Agent。

我见过的优秀回答，不是直接甩出一堆技术名词，而是先画一张五层架构图：接入层、对话管理层、Agent核心层、工具层 、输出管控层。然后逐层讲清楚每一层要解决什么问题，选型依据是什么，trade-off在哪里。

比如记忆模块怎么设计sx？短期记忆放Redis设过期时间，长期记忆用向量数据库存用户画像和历史问题总结。为什么这样选？因为企业场景里，用户问完就走，会话级别的记忆够用就行，但用户偏好和常见问题需要长期保留来提升体验。

第三类，工程落地思维。

Agent开发本质还是应用开发。面试官会追问：幻觉怎么 处理 ，稳定性怎么保证？成本高怎么办？

你不上没有，只有在真实项目里踩过坑的人才能答出来。

二、从面试题看行业真实需求
2026年的Agent s面试，跟传统后端面试完全是两个物种。

后端八股背完就能说，Agent面试你得真做过项目。我总结了一个规律：凡是能讲清楚trade-off的候选人，通过率都高得离谱。

什么叫trade-off？就是你知道ReAct比CoT准确率提升了15%，但你也知道ToT虽然效果更好，token消耗却是三倍，线上扛不住，只能用在离线深度推理场景。这种对效果和成本的平衡感，是企业最看重的能力。

再说一个细 节。有候选人被问到Agent最常见的失败场景，他讲了三个经典case：工具调用失败、上下文溢出、目标漂移。每个case都配了具体的解决方案和兜底策略。面试官当场就给过了。

因为企业招Agent开发，不是招一个会调API的人，是招一个能解决问题的人。

三、这个方向的机会有多大
说几个真实数据。

某电商大厂AI平台部，Agent开发工程师年薪总包在50到80万之间。某大模型独角兽的同岗位，总包能到90万以上。三个offer总包差距近40万，说明同方向不同公司的估值差异极大。

更关键的是，这个方向的人才缺口是真的大。

模型层是巨头在玩的 知道吧，应用层才是普通人的机会。遇到的·1那个人 里那个项目，把个人助手改造成企业级服务，思路并不复杂，但能把这件事做出来并且讲清楚的人，少之又少。

，Agent岗位已 经明显分化为两个方向：算法研究岗侧重创新推理，工程应用岗侧重落地稳定。对于大部分开发者来说，工程应用岗的门槛更友好，机会也更多。

四、普通人怎么入局
如果你现在想转Agent方向，我的建议是三步走。

第一步，先做一个能跑起来 的side project。

不用很复杂。个人知识库Agent、客服对话机器人、智能日程助手，以。关键是完整链路要通：需求分析、架构设计、代码实现、效果评估。面试的时候，你能把这个项目从为什么做、怎么做、遇到什么问题、怎么解决的，完 整讲出来，就赢了一半。

第二步，理解本质，别只背八股。

AI变化太快，背概念没用。比如RAG优化，你要能讲清楚实际在哪用过、解决了什么问题、提升了多少。面试官一听就知道你是真做过还是临时抱佛脚。

第三步，基础工程能 力别丢。

Agent开发再新，也离不开后端基础。三个面试都考了medium难度算法题加系统设计，基础不好直接挂。Redis、MySQL、并发、分布式锁，这些老本行不能丢。



我遇到的 那个候选人的项目，技术角度确实挺有意思。用户隔离、资源隔离、记忆接入mem0、容器化沙箱运行环境，考虑得挺全面。

但更有意思的是评论区的一个观点：这东西做出来，有多少人能买单？

我的看法是，技术价值不需要所有人认可，只需要对的人认可。对于正在找工作的开发者来说，能把这样一个项目从零做出来并且讲清楚，本身就是最好的简历。

AI应用层，机会多得离谱。模型是巨头的游 戏，应用是你我的战场。




上周的有个学员找我复盘面试，情况挺典型的。

前端转AI，卷了半年，RAG链路能从头画到尾，LangChain和LlamaIndex的API倒背如流，Fine-tuning也自己跑过几个小模型。投了七八家AI全栈岗位，前面技术面都过了，有两家甚至面到了三面。

结果总监面全挂。

他自己复盘的时候跟我说，感觉技术问题都答上来了，手撕代码也没问题，项目经历也讲清楚了，实在想不通为什么挂。

我说你把总监面的问题复述一下我听听，听完我就明白了。

技术面考的是会 不会，总监面考的是你想没想

先说说这两轮面试到底有什么区别。

技术面问的是什么。RAG的召回策略怎么选、向量数据库的索引原理、Agent的ReAct循环怎么实现、Prompt Engineering有哪些常用技巧。你答得上来就是答得上来，答不上来就是答不上来，标准很明确。

总监面问的是什么。给你一个业务场景你怎么设计技术方案，你觉得RAG和Fine-tuning各适合什么场景，你之前做的那个项目如果让你重新做一遍你会怎么优化，你觉得AI全栈这个方向未来两年会怎么发展是。

看出来区别没有。

技术面是一个人对着一份考卷答题。总监面是一个人站在一张白纸前面，面试官想看你能不能自己往上画东西。

我带过的学员里，技术面通过率其实挺高的，大家该刷的题都刷了，该补的基础也都补了。但是一到总监面，大部分人翻车的原因高度一致：他们准备了答案，没有准备思路。

你简历上写的全栈，面试官脑海里想的全栈，不是一回事

大部分求 职者理解的AI全栈开发是什么。前端会点React，后端会写Python接口，中间接一个LLM调API，再配个向量数据库，整套链路能跑通，觉得自己就是全栈了。

坦白讲，这个理解放在2024年可能还行。2026年不够了。

你去翻翻BOSS直聘和猎聘上最近的AI全栈岗位JD。企业真正在找的是三种能力。

第一种 ，工程落地能力。不是会用LangChain搭个Demo，是能处理生产环境里的并发、延迟、容错、成本控制这些问题。模型推理一次要2秒，你能不能优化到500毫秒。接口QPS上不去，你能不能定位是向量检索慢还是模型推理慢。

第二 种 ，技术选型能力。面试官给你一个场景，比如要做个智 能客服系统，你选RAG还是Fine-tuning，选Milvus还是Pinecone，选开源模型还是商业API。选完之后能不能说清楚为什么这么选，选了之后要付出什么代价。

第三种，业务理解能力。这个很多人最容易忽视。你做的项目到底解决了什么业务问题，你选的方案怎么跟业务目标对齐的。不是你说用了什么技术，而是你用了这个技术之后，业务的什么指标变好了。

我带过一个学员印象很深。他做了一个企业内部知识库的RAG系统，技术面的时候讲得头头是道，什么分块策略、混合检索、重排序，每一个环节都能展开。到总监面的时候，面试官问了他一个问题，你做的这个系统和直接用ChatGPT上传文件对话，核心区别在哪里。

他愣 住x了，答不上来 其实很多 东西 不难的。就是这样的 只是不熟练

你看，问题就在这。你把自己当一个工程师在回答问题，但总监面要的是一个能从业务价值倒推技术方案的人。这两种思考方式，练的是完全不同的肌肉。

2026年A I全栈的真实行情：不是岗位没了，是门槛变了

我知道写到这里你可能会想，是不是AI全栈这个方向不行了。

不是方向不行，是评价标准在变。

看几个数据。2026年基础模型研发和具身智能方向薪资还在涨，NLP应用开发岗薪资回调了10%到20%。回调的不只是薪资，还有企业的容忍度。以前你搭一个RAG的Demo就有面试机会，现在企业要看你有没有在生产环境跑过，有没有处理过真实用户的反馈。

但反过来看，这也是好事。门槛变高 意味着混日子的人进不来了，真正有积累的人反而更容易被看见。

企业现在用人逻辑很清楚：不迷信论文，看重落地。不追求全栈通吃，要垂直深耕某一两个环节。不找调参工程师，要找能独立设计方案的人。

这个趋势对认真准备的人来说是利好。因为AI全栈这个方向的岗位数量其实没少，少的是那些谁都能干的初级岗位。中高级岗位的需求还在涨，关键是你能不能达到那个门槛。

怎么让你的总监面回答上两个台阶

说回正题。如果你正在准备AI全 栈方向的面试，或者面了几次总监面都挂了，有三个动作可以立刻做。

第一个，把项目经历从功能描述改成决策描述。不要说我的项目用了RAG加向量数据库加大模型，要说我们的业务场景要解决什么问题，我对比了哪几种方案，为什么最后选了RAG而不是Fine-tuning，上线之后核心指标提升了多少。

第二个，准备三个技术选型对比。面试前把你项目里涉及的每一个技术选型都准备一个对比分析。为什么用Milvus不用Pinecone ，为什么用LangChain不用LlamaIndex，为什么用RAG不用直接Fine-tuning。每个选择后面都要有理由，理由后面要有代价，代价后面要有你做的权衡。

第三个，训练自己用一个故事讲完一个项目。不是PPT汇报，不是技术文档，是一个有起因经过结果的故事。起因是业务遇到了什么问题，经过是你怎么选择方案和踩了什么坑，结果是上线之后发生了什么变化。总监面其实就是看你能不能把一个复杂的技术方案，用普通人听得懂的方式讲清楚。

这里

回到正题。总监面挂掉不是能力问题，是表达方式没转到那个频道上。技术面要证明你能干活，总监面要证明你会思考。这两个东西准备的方法完全不一样，但很多人花80%的时间准备技术面，总监面就靠临场发挥。

2026 年的AI全栈市场对真正能独立做方案的人非常友好。门槛变高筛掉了一批人，机会反而更集中了。关键是你能不能抓住这个窗口，把自己的定位从能干活的那个人，变成能想事的那个人。




有个学员拿简历给我看，项目写得挺像回事的。

日均处理订单50万，P99延迟从800毫秒压到80毫秒，Redis集群加了，消息队列也上了，架构图画得也是有模有样。

我看完觉得不错，问了他一句：这个项目中，你具体负责哪个模块的优化？

他愣了一下，说我负责整个后端架构设计。

我说那你讲讲，Redis分布式锁你们用的什么方案，锁超时是怎么处理的，Redisson的底层逻辑有没有看过？

他开始磕磕巴巴地回忆，Redisson好像是用的那个什么……Lua脚本……超时的话应该是有个看门狗机制……

说实话，已经比很多人强了。但这个磕巴，在面试官那里就是不过的理由。

问题在哪？

很多人把面试当成了背书。简历上那些数据背熟了，面试的时候复述一遍，觉得就能过关。但面试官真正想听的，从来不是你做了什么，而是你为什么这么做，以及做的时候遇到了什么。

简历是给机器看的，面试是给人听的
这句话我特别想送给大家。简历过了筛选，是因为HR在找关键词，P99、QPS、Redis、MySQL优化，这些都是信号。但一旦进入技术面试，画风就变了。

面试官坐在你对面，他在找的是你脑子里装的东西。他想看到的是你的决策链条：你遇到了什么问题，你有什么约束，你排除了哪些方案，你最终选了什么，代价是什么。

不是背课文，是重建现场。

怎么重建？我给大家说一个我反复用过的方法，叫做现场重建法。

第一步不是讲你做了什么，而是讲你面临的环境。比如，不要一上来就说"我负责订单系统性能优化"，而是先交代背景：我接手的时候系统日均处理30万订单，P99延迟800毫秒，用户投诉频繁，数据库主库CPU持续80%以上。这个数据一出来，面试官就知道你的处境了，他会有问题来追问。

第二步，讲你做过什么选择，而不是你用过什么工具。

比如，不要说"我使用了Redis分布式锁"，而是说"考虑到库存超卖的后果比较严重，我选择了Redis分布式锁而不是本地锁，代价是增加了约3毫秒的接口延迟，收益是超卖率从0.5%降到了0"。这句话里有选择、有约束、有权衡、有结果，面试官想追问都有一堆可问的。

第三步，永远想清楚你选了什么、代价是什么、放弃了什么。

面试官追问的问题，90%都集中在这一层。你说用了Redis，他想知道你用的Redisson还是Jedis，锁超时怎么办，Redisson的看门狗机制是什么，Lua脚本的原子性怎么保证，集群环境下主从切换会不会出问题。你说分库分表，他想知道你用的是什么中间件，ShardingSphere还是自己写，分片键怎么定的，数据迁移的时候停了多久，停服迁移还是双写方案，跨分片查询怎么处理。

这些问题没有标准答案，但它们背后有一个共同的核心：你在选这个方案之前，还考虑过什么。

这个"还考虑过什么"，才是面试官真正想听到的东西。

为什么你答不上来？因为你根本没准备过
很多人在准备面试的时候，花了两周时间刷算法题，用STAR法则把项目经历背了五六遍，然后上了战场，结果被问懵了。

问题不是你不努力，而是方向搞错了。

面试官不是你的敌人，但他是你的照妖镜。你有没有真正理解这个项目，你有没有想过每个技术选型背后的代价，你有没有对比过不同方案的优劣，他一追问全知道了。

真正的高手在准备面试的时候，是这么干的：

找一张白纸，把项目从0到1的每个关键决策点全部列出来。每个决策点旁边，写清楚你当时为什么选了这个方案，还有哪些备选方案，以及为什么最终没选那些方案。

就这么简单。但就这么简单的一件事，我见过的大多数求职者从来没做过。

他们把时间花在了背八股文上、花在了刷算法题上、花在了练习STAR法则的表达流畅度上。唯独没有花时间，把自己在项目里真正做过的决定想清楚。

结果就是，简历写得漂漂亮亮，面试讲得磕磕巴巴，追问一深入就开始露馅。

这个问题其实出在写简历之前
很多后端开发的简历存在一个很隐蔽的问题：数据看起来很专业，但经不起追问。

比如，简历上写着"日均处理订单100万，P99延迟40毫秒，系统可用性99.99%"，看起来非常漂亮。但面试官问了一句：你这个延迟数据是怎么测的？

答不上来。

这个数据是他照着别人的项目描述抄的，或者是优化完之后随手测了一下，根本没有完整验证过。面试官多问两句，就知道这个数据是怎么来的了。

这不是简历美化的问题，是你对这个项目的理解深度问题。

真正好的简历，每个数据都是你自己测出来的，每个方案都是你自己选的，每个权衡都是你自己做过的。你写上去的东西，是因为你真的懂，你真的能讲，你真的经得住追问。

所以写简历之前，最重要的事情不是想怎么写得漂亮，而是想清楚你在这个项目里，到底有没有扎扎实实地做过这些事。如果某个技术栈你是"参与"而不是"主导"，写的时候就要保守一点。宁可写少一点，也不要写一个自己讲不清楚的东西上去。

三个动作，现在就能做
第一个动作，把你最近做的那个项目的技术决策，全部写出来。每个决策旁边，标注你为什么选了这个方案、代价是什么、放弃了什么。

第二个动作，找一个懂技术的朋友，让他来追问你这个项目。他问得越深越好，如果你每个问题都能回答出来，说明你真的理解了这个项目。

第三个动作，录音复盘。每次面试结束之后，把你回答项目问题的录音回放一遍。你会发现很多自己讲得不清楚的地方，那些地方就是下次要补的缺口。

这三件事看起来笨，但真的管用。

我带过很多后端方向的学员，一开始简历数据都写得挺好看，但面试一讲项目就紧张，说话没有逻辑，追问就卡壳。我们花时间最多的环节，就是一起把每个项目的决策链条重新梳理一遍，找到真正属于自己的亮点，然后反复练习，直到能讲成一个有画面感的故事。

这个过程没有捷径，但方向对了，结果不会差。



AI Agent面试，面试官往深了问，这五个地方一定穿帮


前段时间一个做AI Agent项目的学员来找我，说他的Agent跑起来了，功能看着挺全，但心里没底，不知道面试能不能扛住。

我说你把Trace日志给我看看。看了大概十五分钟，我心里已经有数了。

表面功能齐全，底层五个地方全是硬伤。

一、Guardrail是摆设，0ms放行一切幻觉
他的Trace里，output_check这一步，耗时显示0ms。similarity和factCoverage永远返回1.0。

事实核查根本没跑，返回值是后端写死的Mock数据。

模型胡编乱造的时候，系统照样判定"事实覆盖率100%"。

二、意图识别全是"通用问答"，没有路由
问代码问题、问个人数据、问实时营收，intentLabel全是同一个。

没有Router节点，只有分类标签，没有路由表。

所有问题硬套同一个管道，该走代码检索的走了通用问答。

这是Chatbot，不是Agent。

三、检索只拿Top-1，一错全错
web_search每次只返回1条结果，sourcesCount只有1到2。

直接取Top-1结果，没有多源召回和冲突检测。

问"2026年营收"这种易错问题，唯一的源错了，答案就错了。

他答不上来"来源冲突时怎么办"，因为根本没想过。

四、Trace是一条直线，没有Agent架构
Trace结构永远是 Intent → Guardrail → RAG → Output。

一条直线，没有分支，没有工具调用痕迹，没有记忆读写节点。

用户让"列出所有Functions"，系统没有function_call痕迹。

没有Planner、没有Executor、没有State Machine。

五、整个项目只有RAG有3个数字，其他全定性
简历里写了召回率82%、精确率78%、幻觉率15%。

但只有RAG有指标。Guardrail没有拦截率，路由没有准确率。

多源没有一致性率，端到端没有完成率。

面试官问"评估体系是什么"，他只能讲RAG，其他环节全是"做了三层防护"。

怎么修
Guardrail 接入NLI语义校验，替换硬编码，拦截率做到85%以上

意图 用LLM做Router，分Code/Fact/Memory/General四类，准确率90%以上

检索 强制top_k至少3条，冲突时标注争议，一致性率80%以上

架构 定义Tools，增加Memory节点，复杂问题先Thought再Action

评估 每个环节都要有量化指标，定期用1000条以上测试集跑回归

Guardrail看拦截率和误拦率

路由看准确率和Fallback率

多源看一致性率和冲突检测触发率

端到端看完成率和响应时间

最后
Agent项目不是搭完功能就完事。

面试官看的是你的Guardrail有没有真实运行，不是硬编码的1.0。

看的是意图有没有路由，不是全走通用问答。

看的是检索有没有多源验证，不是Top-1就敢回答。

看的是有没有Agent架构，不是一条直线的Chatbot。



上个月我连续面了字节、腾讯、阿里三家的大模型应用和AI Agent开发岗。面完坐在回去的车上，脑子里只有一个念头，同样叫Agent开发岗，三家问我的问题居然完全不在一个层面上。

这不是个别现象。2026年Agent岗已经成了大厂AI方向招人的主力方向，但每家对Agent的理解、考核重点、甚至认为什么是好答案，差异大到让人措手不及。

今天把这三家的面试逻辑拆开讲讲，如果你也在准备这个方向的面试，看完至少能少走一半弯路。

字节跳动，追的就是工程实现细节
字节的Agent面试，技术密度是三家里最高的。

面试官不会问你Agent是什么，直接追问实现细节。比如ReAct框架，他不会让你背定义，而是问，你的ReAct循环里消息格式怎么设计的，tool_response用user角色还是assistant角色传回，为什么。

这个问题答不对，后面基本不用继续了。

正确答案是tool_response要用user角色传回。因为它是外部系统返回的内容，不是模型生成的。如果放assistant角色，模型会以为自己说了这些话，后续推理就乱了。

再看训练流程这道题，字节高频考Agentic CPT到SFT到RL三阶段，还会追问，为什么SFT时要mask掉observation tokens。

因为observation是外部环境返回的结果，不是模型生成的。如果对observation也算loss，模型会试图学习预测搜索结果，这件事既不可能也没有意义。

字节还有一个经典追问，你的Agent调了三个工具就死循环了，异常处理写在哪。

这道题考的不是背概念，是你有没有真正跑过生产级的Agent。答案要能说出来三层，最大迭代次数硬限制、循环模式检测、异常回退策略，重试然后降级最后人工介入。

所以面字节，准备重点很明确，ReAct的工程实现从头写到尾，不依赖任何框架，从system prompt设计到tool_call解析到tool_response回传，完整跑一遍。还有Agent训练流程，SFT时哪些token要mask，RL时奖励函数怎么设计。异常处理和日志追踪，这个是区分背概念和真做过的关键。

腾讯，协议和生态理解才是真正考点
腾讯的面试风格不太一样，更偏工程协议和生态视野。

面试官会问，Workflow和Agent到底有什么区别。看起来简单，大多数候选人答不好。

Workflow是规定动作，控制流是代码确定的，走哪条路程序员写在代码里了。Agent是自由体操，每一步走哪由LLM自主决策。最好的实践是两者组合用，用Workflow兜底，用Agent处理异常和动态分支。

然后会问MCP和A2A了解多少。很多候选人一听就懵了，觉得Agent不就是LLM加几个工具调用吗，要这么多协议干什么。

2026年的现实是，Agent已经不是一个模型加几个工具那么简单了。MCP是标准化工具连接协议，相当于USB-C接口，让Agent能插各种工具。A2A是Agent间通信协议，相当于HTTP，让多个Agent能互相通信和协作。

如果你去面腾讯的Agent岗，连MCP和A2A都讲不清楚，面试官基本就判定你没有真正做过Agent生态，只是在自己的笔记本上跑过几个toy example。

腾讯对记忆机制的考察也很系统，短期记忆怎么管，长期记忆怎么存，上下文超出限制了怎么处理，是用滑动窗口还是动态摘要。这些问题的答案不在任何一篇公众号文章里，在你真的设计过记忆系统之后才会有的体感。

阿里巴巴，架构格局和业务落地都要
阿里的面试是三家里最求全的，既要架构格局，又扣落地细节。

有一个题几乎必出，Tools、Workflow、Agent三者的区别到底是什么。答不清楚，面试官直接就在心里把你归到不合格那档了。

这三个东西根本不在一个层次上。Tools是零件，是搜索、计算、读文件这些原子能力。Workflow是流水线，先搜索再分析最后输出，路径是固定的。Agent是能换流水线的机器人，根据自己的判断决定下一步做什么。

然后阿里的Multi-Agent架构题，要求你画出三层结构，路由层、管理层、执行层，还要说清楚Agent之间怎么通信，怎么避免死锁和死循环，结果怎么聚合。

这些都是有标准答案的，通信靠A2A协议，死锁靠超时加心跳加终止信号，结果聚合有投票、权威仲裁、分层汇总几种方式。

阿里还比较务实，会直接问，你做了这么多Agent项目，落地最大的瓶颈是哪几个。

2026年最真实的答案是这三个，推理断层导致多步推理中信息丢失，结果对齐问题导致Agent自主决策和业务规则冲突，可观测性不足导致Agent决策过程是个黑盒，出了问题查都不知道从哪查起。

三家对比，看看你在和谁对话
把三家放在一起看，差异就非常清楚了。

字节追技术深度，你要能讲清楚每一个设计决策背后的原因，不能只说用了什么，要说为什么用这个不用那个。

腾讯追工程视野，你要对Agent生态有全局理解，协议、标准、工具链，不能只知道自己那一块。

阿里追架构和落地，你要既懂分层设计，又知道真实业务里哪里会卡住。

代码块
PlainText
自动换行
复制代码
考核维度对比
==========================================
          技术深度  协议广度  架构格局  落地细节
          ──────  ──────  ──────  ──────
字节跳动    █████   ███      ███      █████
腾讯        ███     █████    ███      ███
阿里巴巴    ███     ███      █████    █████
==========================================
复制成功
这个表不是精确打分，但能帮你快速判断，你准备的方向更贴近哪家的考核逻辑。

说到这，插一句话。如果你现在正在找工作，或者想提前做准备，需要简历优化、面试辅导、求职陪跑这块的帮助，可以去置顶动态里找到联系方式，找云老师聊聊。不少同学改完简历加上面试辅导一轮下来，面试邀约率和最终通过率还是提升了不少。

一个让很多人挂掉的误区
聊到这，我要说一个很多候选人都在犯的错。

大家准备Agent岗面试的时候，习惯把所有公司的考点混在一起准备，背同一份题库，写同一份项目介绍。

这件事的后果是，你看起来什么都懂一点，但每家的追问一深就露馅了。

字节要听你讲工程细节，你给他讲生态愿景。腾讯要听你讲协议设计，你给他讲训练流程。阿里要听你讲架构分层，你给他讲工具调用格式。

不是你不懂，是你说的话和面试官想听的不匹配。

准备的时候，最好先定好你要面哪几家，然后针对性地准备。面字节就把ReAct工程实现完整写一遍，面腾讯就把MCP A2A几个协议的区别和联系讲透，面阿里就画一个完整的Multi-Agent架构图，说清楚每层做什么，层之间怎么通信。

2026年的机会，比很多人想的多
说到这，肯定有人会想，这个方向是不是太难了，要求这么高，还有机会吗。

我直接说结论，2026年是Agent开发岗机会最集中的一年，没有之一。

大厂在大规模招人，创业公司在疯狂招人，传统企业在数字化转型也在招人。需求是真实存在的，而且不是那种为了赶热点招两个人的需求，是真的很缺能把Agent系统从设计到落地全流程搞定的人。

真正的问题不是机会不够，是符合条件的人太少。

这也是为什么，同样的市场，有人投了几十份简历一个面试没约到，有人面了三家拿了三个offer。差距不在学历，不在背景，在有没有真正理解这个方向在考什么，有没有做过完整的、能讲清楚决策过程的项目。

几个能立刻做的动作
文章最后，给你几个今天就能做的动作。

第一，去拉一下字节腾讯阿里三家最近招的Agent岗JD，对照看一下哪家的考点和你的背景更匹配，不要盲目海投。

第二，选一个方向深挖，要么把ReAct工程实现完整写一遍，要么把MCP A2A协议搞清楚，要么设计一个完整的Multi-Agent架构。不要什么都懂一点什么都不精。

第三，找一个真实场景做一个完整的Agent项目，不是toy example，是能跑起来、有真实用户、能讲清楚每一步决策原因的项目。面试的时候，讲这个项目比背一百道面试题都有用。


前几天跟一个做AI应用开发的学员聊，他跟我说了一件事。

去年年底他把简历投出去，大模型应用开发相关的岗位，投了三十多家，面试邀请拿到了两个。

他觉得很奇怪，自己明明做了两个RAG项目，一个Agent项目，LangChain用了，向量数据库用了，Prompt工程也写了，关键词一个不少，怎么会没面试呢。

我拿他的简历看了一下，看完我就知道问题在哪了。

他的项目描述是这样写的：

负责RAG系统的开发与优化，使用LangChain框架，接入向量数据库，实现文档检索与生成功能，使用GPT-4进行答案生成，系统准确率达到85%。

这一段，一看就是AI帮他写的，或者说，写的时候根本没想过面试官会怎么问。

面试官看项目经历，到底在看什么
很多人以为，面试官看项目只是为了确认你用过这些技术。

不对。

面试官看项目，是为了判断一件事：如果让你来我们这边做类似的事情，你能不能独立扛下来。

这两个是完全不同的逻辑。

用过某项技术，不等于能用这项技术解决实际问题。

我接触过不少做大模型应用开发的面试官，他们告诉我，筛简历的时候，最怕看到两种项目描述。

第一种，堆技术名词，没有任何业务背景。

第二种，有业务背景，但看不出候选人自己做了什么决策。

这两种描述，本质上暴露的是同一个问题：你只是在跟着教程走，而不是在解决实际问题。

大模型应用开发，项目描述的三层结构
写好一个大模型应用开发的项目经历，核心是要让读者看懂三件事。

第一层，你面对的是什么问题。

不是做一个RAG系统这种表述，而是具体的业务场景。举个例子，每天有300多条用户反馈需要人工归类，平均处理时间超过4小时，准确率只有70%，业务方希望用AI来提升效率。

有这个背景，后面的技术选型才有意义。

第二层，你做了哪些关键决策。

这是整个项目描述里最重要、也最容易写砸的部分。

很多人的写法是，列技术栈。LangChain加Chroma加GPT-4加FastAPI，然后就没了。

面试官看到这种描述，下一个问题一定是：为什么选Chroma而不是Milvus？为什么用LangChain而不是自己封装？GPT-4的cost你们是怎么控制的，有做缓存吗？

如果你写的时候没有想过这些问题，面试的时候就一定答不好。

所以项目描述里，要把关键决策点写出来。不需要写很长，但要让读者感受到，这些选择是你经过比较之后做出的，而不是跟着某篇教程抄的。

第三层，结果是什么，怎么衡量的。

这是大多数人的弱项。

准确率提升到85%，这是一个开始，但不够。

面试官想看到的，是你知道怎么定义成功。

准确率是在什么测试集上测的，测试集多大，覆盖哪些场景。除了准确率，延迟怎么样，一次查询平均多少秒。成本呢，每天跑下来API费用多少，有没有做优化。

这些数据不需要非常精确，但要有。

没有量化指标的项目描述，面试官默认打个问号。

一个真实改写示例
前面提到的那个学员，我帮他把项目描述改了一下。

改之前就是前面那段，技术名词堆在一起，没有任何决策过程和量化细节。

改之后是这样的思路：

先说背景，他们公司有一个内部知识库，几千篇技术文档，工程师查问题的时候需要用关键词搜索，命中率很低，大家抱怨比较多。

然后说他的做法，他调研了三种方案，关键词搜索增加语义检索，直接上RAG，外接一个专门微调过的问答模型。他选了RAG，原因是部署成本低，可以先用起来，后续再迭代。

接着说具体实现，文档切分策略试了三种，按段落切、按固定字数切、按语义边界切，最后用的是语义边界，因为他们的文档结构比较规范。向量模型选型，对比了OpenAI embedding和本地部署的bge-large，最终选了bge-large，原因是可以本地部署，不需要每次都调外部API，延迟更可控。

最后说结果，召回准确率从原来的30%提升到78%，平均查询时间1.2秒，工程师使用满意度调研打分从2.8上升到4.1。

这个版本的项目描述，面试官拿到手，能问出有深度的问题，而候选人也能接得住。

这才是好项目描述应该达到的效果。

不同经验阶段，侧重点不一样
如果你是应届生或者刚转行，没有真实的生产项目，那就把学习项目写深。

什么叫写深？

不是把教程里的步骤列一遍，而是把你在做这个项目过程中的思考写出来。

你为什么选这个数据集，遇到过什么坑，怎么解决的，如果重新做一次你会改哪些地方。

这些东西，面试官一看就知道你是不是真的做过，还是只是把GitHub上的代码跑了一遍。

如果你有1到3年经验，面试官更看重你把技术变成产品的能力。

模型效果不错，怎么让它稳定地跑在生产环境里。

延迟、成本、监控、降级策略，这些工程化的问题，如果你在项目描述里完全没提，面试官会默认你没想过。

如果你有3年以上经验，面试官希望看到的是架构思维和判断力。

技术方案的取舍，为什么这么做，不考虑那个方向的原因是什么，如果业务规模扩大10倍你的架构需要改哪些地方。

这些问题，在你的项目描述里应该能找到线索。

2026年，大模型应用开发还有机会吗
很多人问我这个问题。

我直接说结论：机会不但有，而且可能是近三年来技术岗位里机会最集中的方向之一。

为什么？

因为落地速度在加快。

2025年的时候，大家还在讨论RAG到底靠不靠谱，Agent会不会只是个概念。到了2026年，已经有大量中小公司真金白银地在往这个方向投，招聘岗位在变多，要求的岗位级别也在变宽，从原本只招senior，到现在junior和mid-level也开始有需求。

但机会变多，不代表门槛变低。

恰恰相反，面试官的判断标准在提高。

2025年初的时候，你只要在简历上写用过LangChain、知道RAG是什么，就很容易拿到面试。

现在不行了。

面试官见过的候选人多了，知道哪些是真做过的，哪些只是跟教程走了一遍。

所以你现在如果准备入这个方向，或者准备跳槽，最核心的一件事就是：把你的项目经历吃透，做到面试官随便问哪一层你都能接得住。

这不是刷面试题能解决的。

是要真的理解你做过的东西。

说点实际的
这里插个福利。如果你现在正在找工作，或者想往大模型应用开发这个方向转，需要简历优化、面试辅导、求职陪跑相关的帮助，可以在置顶动态里找到联系方式，找云老师聊聊。很多同学改完简历和面试辅导之后，面试邀约率和通过率提升还是挺明显的。

大模型应用开发这个方向，说实话，比很多人想象的要宽。

不只是做RAG和Agent，还包括Prompt工程、模型微调、推理优化、多模态应用，甚至AI产品的设计与评估，都是这个方向里能深耕的细分领域。

如果你本来就做后端或者全栈，转型大模型应用开发的坡度没有你想象的那么陡。

你已有的工程能力，在AI应用开发里非常值钱，因为大多数只懂模型的人，工程化能力是短板。

反过来，如果你本来就做算法或者模型，那补一下工程这块，你的竞争力会强很多。

最怕的是，哪块都只知道一点，但哪块都经不起深挖。

简历上的项目经历，就是面试官判断你属于哪类的第一个窗口。

把这个窗口擦亮，后面的机会才会真的向你打开。


我最近连着看了十几个AI开发方向的简历，说实话，大部分都一个问题。

技术名词铺了半页纸，LangGraph、RAG、Multi-Agent、LoRA微调，看着挺唬人，但往深了问两句就穿帮。

不是没做过项目。是真的做过。只是不会写。

这个事情我越琢磨越觉得值得好好聊聊。2026年AI Agent这个方向，HC确实在涨，但投递的人更多。HC投递比大概1比200，你说卷不卷。

但好消息是，这200个简历里，有180个都栽在了同一个地方，就是项目经验的写法。你把这块搞明白了，在起跑线上就赢了一大半。

今天不聊虚的，拿两个我辅导过的学员项目当案例，跟你说说AI Agent方向的简历到底该怎么写。

面试官到底在看什么
先说一个很重要的事。你以为面试官在看你的技术栈，其实不是。

LangChain熟不熟、LangGraph会不会用、RAG调过没，这些问题写在简历上只能说明一件事，你把文档读完了。

真正的分水岭在一句话上。

你说你做了Agent系统，那问一个问题。你这个Agent为什么拆成五个而不是三个？

你要是能说清楚这个选择背后的理由，这场面试就已经过了60%。你要是只会说我用LangGraph搭了个五Agent系统，然后等着面试官继续问，那就悬了。

很多人不理解这个逻辑。觉得面试不就是要答对问题吗。但AI Agent这个方向的面试，没有标准答案，面试官想看的是你面对一个开放问题时的思考和判断。

你选BM25做检索而不是向量检索，为什么。你选DoRA做微调而不是全量LoRA，为什么。你用GRPO做强化学习而不是DPO，又是为什么。

每一个为什么背后，都是你项目含金量的支撑点。

简历里的Agent项目，别再用技术名词堆了
坦率的讲，我在做辅导的时候见过最典型的Agent简历，长这样。

基于LangGraph搭建Multi-Agent系统，使用RAG实现知识检索，通过Function Calling调用外部工具。

没了。就这些。

你看着觉得写得还行对吧，但这恰恰是面试官看完就划走的写法。因为没有任何信息量。

什么信息才叫有信息量，我给你一个对比。

反面写法，基于LangGraph搭建Multi-Agent系统，实现了任务分解和工具调用。

正面写法，基于LangGraph搭建五Agent协同系统，包含意图识别Agent（准确率92%）、任务路由Agent（分流准确率88%）、以及三个执行Agent，通过Jieba分词+SQLite BM25索引构建混合检索链路，召回率从45%提升至78%，整体系统准确率88%。

下面也是我给学员改的案例


看到了没。同样是写Multi-Agent，同样的项目，调整一下表达方式，读起来就是两个层次。

核心差距在哪，每多一个数字，简历的信任感就翻一倍。78%和88%这两个数字摆在这里，面试官脑子里的画面就从这个人调了个框架变成了这个人真的做了一套能用的系统。

代码块
PlainText
自动换行
复制代码
简历含金量梯度：
█ 技术名词堆砌      30分（知道这个技术存在）
██ 加上做了什么      50分（用过这个技术）
████ 加上量化结果    80分（真的做出效果了）
██████ 加上选型理由  100分（有独立判断力）
复制成功
说到选型，这是第二个关键层。你检索为什么选BM25不选向量检索，你在什么场景下做的决策，这个决策带来了什么效果。这些是大多数简历里完全没有的东西，但恰恰是面试官最想看到的独立思考。

微调项目怎么写到简历里才算过关
到现在很多做AI开发的人简历里都会写上一条微调经验，SFT、LoRA、RLHF这几个词像标配一样挂在那里。

但我问你一个问题。你简历上写的微调，面试官看完之后，能回答这三个问题吗。

训练数据怎么来的，多少条，怎么构造的。

超参怎么选的，LoRA rank设多少，为什么是这个数。

效果怎么评估的，跟什么基线比的，提升了多少。

如果答不上来，那这个项目在简历上就是白写的。

说一个具体的例子。最近有个学员在简历上写基于Qwen2.5-7B做领域模型微调，我帮他重新改了一版简历，长这样。


基于Qwen2.5-7B，采用DoRA低秩适配+SFT+GRPO强化学习三步训练链路。SFT阶段构造5000条领域数据，有效数据占比89%。GRPO阶段人工评分较SFT基线提升23%。部署端使用BitsAndBytes 4-bit NF4量化，显存降低43%，VLLM推理吞吐量提升35%，首Token延迟1.2秒。建立反馈分表机制，支持月度DPO持续迭代。

这个写法厉害的地方在哪。从头到尾，每一个技术动作后面都跟着一个具体的数字。数据量、提升幅度、延迟时间、迭代频率。面试官看完不需要猜你做了什么，他可以直接追问细节。

这里插个福利。如果你现在正在找工作，或者想提前准备，需要简历优化、面试辅导、求职陪跑相关的帮助，可以在置顶动态里找到联系方式，找云老师聊聊。很多同学改完简历和面试辅导之后，面试邀约率和通过率提升还是挺明显的。

海云日记：提供前端开发/后端开发/AI全栈开发/AI开发（大模型应用/AI应用/AI Agent）求职辅导，提供简历优化、简历面试辅导、求职陪跑。

面试碰到这三道题，答好就稳了
简历写好了，接到面试邀请，接下来怎么准备。

AI Agent方向的面试，这三道题几乎是必考的。而且每一道都是开放性场景题，没有标准答案，考的就是你项目做得深不深。

第一道，如果让你设计一个客服Agent，任务怎么拆，记忆怎么管，出错怎么兜底。

这道题考的就是Agent架构设计。你能不能把复杂任务拆成合理的粒度，能不能说清楚每个Agent的职责边界，能不能考虑到长对话场景下的记忆管理和上下文压缩。嘴一快就往外蹦一句ReAct的，基本就挂了。

第二道，你为什么要用RAG而不是微调，什么时候该微调什么时候该RAG。

这道题考的是工程判断力。不是技术选型对错的问题，是你有没有在真实场景下做过这个决策，有没有被坑过。说得出具体场景和取舍逻辑的，跟背课文一样说RAG适合知识更新频繁的，面试官完全能听出来。

第三道，你的Agent系统准确率是怎么评估的。

这道题最见功底。多数人会说，我用人工评估了一下然后就没了。但真正做深的人会告诉你，他评了什么维度，样本量多少，跟什么基线比，各个模块分别是什么指标，哪些场景容易出错，第二轮迭代改了什么。

能把这三点答扎实了，AI Agent这个方向，2026年机会真的很大。

越是卷的时候，越要想清楚卷什么
最后说几句真心话。

前面聊了这么多Agent简历和面试的东西，其实想表达的就一个意思。2026年这个赛道不是没机会，是机会的门槛变了。

以前会调个RAG就能找到AI开发的工作，现在不行了。但你换个角度想，那些真的有深度思考能力、真的把项目做透了的人，在简历关就已经跟大部分竞争者拉开了距离。

我有时候觉得，AI Agent这个方向特别像几年前的移动端开发。早期大家拼谁会画界面，后来拼架构设计，拼性能优化，拼复杂场景下的工程能力。现在的Agent开发正处在从会用到能用到好用的转折点上。

能跨过去的人，就是下一个周期的赢家。



老师分享下经验，因为我最近在一个在线协作平台做了个 AI 助手，让团队用自然语言管项目。

比如：帮张三建个任务、这个迭代进度怎么样、哪些需求延期了，系统听懂后就自动执行。

做到现在，57 个意图，7 个业务域，准确率从 86% 干到了 97%。

这篇文章是我从头到尾踩过的坑和改过的方案。

第一阶段，把所有希望寄托在一个 prompt 上
最初的方案很直接。把所有意图的名称和描述塞进一个 prompt，让 LLM 返回匹配结果。一个 prompt 同时做三件事：识别意图、提取参数、输出置信度。

实际跑起来，两个问题立刻暴露。

慢。50 多个意图的完整描述塞进 prompt，大概 5000 token，LLM 每次调用 3 到 4 秒。对话系统的用户期望秒级响应，等不了。不准。选项太多，LLM 经常在语义相近的意图之间漂移，用户说任务情况，它可能匹配到客户查询、工作记录、数据统计中的任何一个。

我把 50 多个意图按用户心智模型划成 7 个语义域，项目、任务、资源、迭代、团队、通知、数据，每个域配一组关键词。用户输入先过关键词匹配，确定落在哪一两个域，只把该域的 5 到 10 个意图送进 LLM。同时把 prompt 格式从多行描述改成单行紧凑格式，每个意图从大概 100 token 压到 40 token。再把意图识别模型从通用推理模型换成速度更快的轻量模型。

效果立竿见影。延迟从 3 到 4 秒降到 0.5 到 1.2 秒，准确率也涨了，候选少了，LLM 更容易选对。

但这带了新问题。

第二阶段，域预筛选，从 3s 降到 0.5s，但漏词是无底洞
域预筛选靠关键词，而关键词永远不够全。

每次联调都能发现新漏词。进度这个高频词，我没放进任何域的词库，任务情况、工作记录全部匹配失败。今天团队进度怎么样，没命中数据域也没命中任何任务锚点，走了闲聊。完成率在数据域的词库里，但不在任务型意图的锚点里，经营报告任务没被注入候选。

更头疼的是，你不知道你漏了什么，直到用户说了那句话。

除了漏词还有误伤。我设了怎么操作、怎么做这些关键词匹配操作指南类意图。结果下个迭代怎么提效率被怎么做三个字命中，路由到了操作指南检索，但这明明是个经营策略问题，不是系统操作教学。

我搞了三层防御。负向排除，给容易误伤的关键词加排除模式，怎么做命中操作指南，但怎么做能、怎么做才能这种模式排除掉。多域叠加，一个关键词可以命中多个域取并集，进度同时指向迭代域和数据域。全量兜底，如果没有任何域命中，不拒绝，用全量意图压缩格式做一次识别，宁可慢一点也不漏掉。

但说实话，这只是在打补丁。关键词方案的上限就在那里，能解决 70% 的路由问题，剩下 30% 需要语义理解。这个认知为后面的 embedding 通道埋下了种子。

回头看，这个阶段最重要的收获不是关键词怎么配，而是意识到基于规则的召回和基于语义的理解是两种根本不同的能力。后来的双通道架构，起点就在这里。

第三阶段，架构分层，让 LLM 只做理解，代码做决策
做了域预筛选之后准确率提升了，但一类问题始终解决不好：LLM 同时做理解和决策，两者逻辑互相打架。

具体来说，我的 prompt 要求 LLM 做三件事：识别涉及哪些意图，做 NLU；给出置信度，低于 0.6 就走闲聊，做路由决策；判断是单意图还是多意图需要编排，做编排决策。

结果很糟糕。

置信度是个坑。LLM 对任务型意图，利润分析、热销排行，经常给 0.4 到 0.5 的置信度，不是没识别到，是对自己不确定。但我的阈值 0.6 把它们全拦掉了，一个阈值干掉了一半的任务路由。多意图检测不稳定，用户说建个项目再拉个群聊，LLM 应该识别出两个意图，但它经常直接匹配第一个，把第二个吃掉。

业务判例越堆越多。为了修单个 case，我往 prompt 里加规则：任务情况转数据、创建活动不匹配查询、入库和出库不要混淆。prompt 从 10 行规则膨胀到 40 行，每修一个 case 可能引入两个 regression。就是在用自然语言写 if-else。

核心改法很简单，LLM 只做语言理解，路由决策交给代码。

旧流程是 LLM 直接输出路由结果和编排判断。新流程里 LLM 只输出这句话涉及哪些意图，代码根据 len(intents) 做编排，根据结构化评分做路由。

具体改了三件事。

Prompt 瘦身，删掉全部业务判例规则，从 40 行砍到 5 行。LLM 只需要输出这句话涉及哪些意图，不需要做任何路由判断。

结构校验层，在 LLM 输出之后路由之前加一层确定性校验。每个意图预先声明它的动词类型，query、write、report、analyze，和参数语义模式，人名、手机号、金额、日期。代码根据声明做评分排序：score = 必填覆盖乘以 0.50 加 参数模式匹配乘以 0.30 加 动词匹配乘以 0.15 加 LLM 置信度乘以 0.05。

注意 LLM 置信度只占 5%。主决策靠结构化信号，LLM 置信度降到微调级别。

四态裁决：CLEAR，唯一候选或分差明显，直接执行；AMBIGUOUS，多候选分差小，追问用户或编排；INSUFFICIENT，候选确定但缺参数，槽位追问；REJECTED，全部淘汰，域引导拒绝或闲聊。

这一步改完之后，prompt 里的业务判例全清了，但识别准确率反而提升了，因为原来那些规则本来就互相打架。

我后来把这阶段总结成一条原则：系统中规则的唯一合法形式是结构化规则，基于 schema、类型、模式的判断。在 prompt 里用自然语言写的业务判例，说到底就是不可维护的 if-else。



如果你正在准备简历或面试，需要简历优化、面试辅导或者求职陪跑，可以在置顶动态找我。前端、后端、AI 全栈、AI Agent 应用开发这几个方向都可以。

第四阶段，问题分流，不是所有输入都该进意图匹配
架构分层之后意图匹配链路本身稳定了很多，但一类问题暴露了：有些用户输入压根不该进入意图匹配。

三个典型 case。建项目怎么操作，被匹配到了建任务意图，缺参数开始追问任务ID和金额，但用户想问的是怎么操作系统，不是要执行建任务。下个迭代怎么提效率，被匹配到了利润分析任务或操作指南，但这是个经营策略问题。分析一下，被匹配到了经营策略建议，但用户刚查了某个客户的详情，说的是分析一下这个客户。

这三个问题的共同点是，它们需要在意图匹配之前先回答一个更基本的问题：这是不是一个技能问题。

在意图识别之前加一层 Problem Type 预分类，把用户输入先分成五类。ACTION，明确执行动作，如帮张三建个项目；QUERY，明确查数据，如查一下这个迭代进度；GUIDE，问系统怎么操作，如建项目怎么操作；STRATEGY，问经营建议，如下个迭代怎么提效率；CHAT，闲聊，如你好。

分类器两步走：先用规则，关键词加模式匹配，规则不确定时才调一次轻量 LLM，不注入意图列表只判断问题类型。

这样，操作教学和经营策略在入口就被分流了，不再和业务意图竞争。

这一步揭示了一个容易被忽视的前提：不是所有用户输入都适合走意图路由。强结构任务如建任务发货适合；弱结构查询如分析数据适合但需要更强的推理；开放问题如经营策略操作教学根本不该进入意图竞争。先判断这是不是一个意图问题，比匹配哪个意图更重要。

但 case 3 分析一下还没解决。单看这四个字，Problem Type 判定为 STRATEGY 完全合理。问题在于它不感知对话上下文。所以又加了一层上下文拼装：在分类之前先检查对话中是否有刚解析的实体，比如刚查了某个项目。如果有，把实体信息拼入分类输入，分析一下变成分析一下某个项目，Problem Type 就能正确判定为 QUERY。

第五阶段，三模型对比，发现单模型天花板
做了上面所有优化之后，我决定用数据说话。写了 104 条标注路由 case，覆盖正向匹配、边界表达、混淆干扰三类，跑了三个不同能力等级的 LLM。

轻量快速模型，准确率 80.8%，平均延迟 2.2 秒。中等均衡模型，准确率 82.7%，延迟 6.0 秒。强力推理模型，准确率 86.5%，延迟 15.1 秒。

三个关键发现。

中等模型性价比极低。比轻量模型只提高 1.9%，延迟翻了 2.7 倍。意图识别不需要强推理能力，用贵模型是浪费。

数据分析域是最大的失败点。轻量模型在分析域只有 47% 的准确率，强力模型有 87%。利润分析、热销排行、延期预警这类任务型意图需要 LLM 做语义归纳推理，简单的关键词匹配和模式识别不够用。

没有一个模型突破 87%。27 条不同的失败 case 中，只有 4 条是三模型共同失败的，也就是说 73% 的失败是模型特异的。每个模型有自己擅长和不擅长的场景。

也就是说，如果能让不同能力互补，准确率应该能显著提升。

把失败 case 按根因分类，发现了五种模式。任务型意图被误判为闲聊，占大约六成，LLM 给低置信度被阈值拦掉。短词命令被误判为操作指南，占大约一成五，规则层过早拦截，LLM 没机会处理。跨域语义歧义，占大约一成。口语化表达不识别，调个期、人够不够，占大约一成。歧义误判为多意图，占大约半成，查一下这个任务被当成 8 步编排。

占比最大的前两类，说到底也是同一个问题：单通道方案的天花板到了。纯 LLM 方案语义推理强，但置信度不稳定，对短词和口语表达识别弱。如果换成纯 embedding 方案，关键词匹配好，但间接表达不行，这个任务取消掉匹配不到取消订单。

这直接指向了最终方案。

第六阶段，双通道融合，突破天花板
我把最终方案总结为三层：Intent Resolution = Recall 召回加 Reasoning 推理加 Ranking 排序。

Recall 从候选池中快速找出可能相关的意图，用 embedding 向量检索。Reasoning 对候选做语义推理和消歧，LLM 在意图树上推理遍历。Ranking 多信号融合评分，输出最终判决，alpha 加权排序加三态决策。

这个结构不只适用于意图识别。RAG 里的文档选择、Agent 里的工具选择、推荐系统里的候选排序，说到底都是同一个问题：多信号转统一评分再排序决策。只是每个场景的 Recall 和 Reasoning 实现不同。

在意图识别这个具体场景下，Embedding 做 Recall 和 LLM 做 Reasoning 各有优劣，互补性极强。用户说取消订单，纯 Embedding 能命中 cancel_task，纯 LLM 也能命中。用户说这个任务取消掉，纯 Embedding 匹配不到，纯 LLM 能命中 cancel_task。用户说今天团队进度怎么样，纯 Embedding 走到 query_revenue，纯 LLM 走到 daily_report。用户说什么卖得最好，纯 Embedding 走到 query_product，纯 LLM 走到 hot_selling。

两个通道并行执行，结果融合为三态判决。Embedding 通道 5 到 15 毫秒跑完，意图树推理通道走 LLM 调用，两个并行。融合评分 final = alpha 乘以 embedding_score 加 (1 减 alpha) 乘以 tree_score。

通道一用 sentence-transformer 模型，bge-small 系列，大概 95MB，将用户消息和每个意图的描述编码为向量，cosine similarity 排序取 top-K。速度极快，关键词匹配好，但间接表达不行。

通道二受 VectifyAI 的 PageIndex 框架启发，将意图识别建模为 LLM 在层级树上的推理遍历。不是把 50 个意图平铺给 LLM，而是构建一棵按语义域分组的树。项目管理下面挂 check_task、create_task、task_analysis，经营分析下面挂 daily_report、profit_analysis、hot_selling。LLM 用 CoT 推理，先判断属于哪个域，再在域内精确匹配。语义推理强，但依赖 LLM 可用性和响应速度。

这棵意图树就是一个轻量的语义知识图谱：domain 是上层语义聚类，intent 是节点，tree_text 定义的不只是这个意图是什么，更关键的是它不是什么，通过描述与相邻意图的边界，给 LLM 提供了推理路径。这和传统的 flat list 分类有区别。

生产中最核心的一个 insight：一段描述不可能同时对 embedding 和 LLM 都最优。

一个意图需要配两段文本。embed_text 面向 embedding，关键词密集，同义词丰富，喂给 embedding 模型用户实际会说的话：热销排行, 做得最多, 高频任务, 什么任务多, 爆款, 经常做。tree_text 面向 LLM，描述性强，含消歧提示，告诉 LLM 这个意图是什么以及不是什么：用户想看任务完成排行，区分，查完成率走 daily_report，查成员效率走 staff_commission。

在 104 条标注 case 上做网格搜索找最优参数。alpha 最优值 0.10，embedding 权重极低，意图树推理主导。delta 最优值 0.10，Top1 和 Top2 差距阈值，低于此值判定为歧义。

alpha 为什么是 0.10？在我当前的数据集上 57 个意图，LLM 推理能 cover 大多数场景，embedding 更多是召回安全网的角色。但这个比例不是普适的，如果意图数量增长到 200 加，或者扩展到多行业、多语言场景，LLM 会逐渐力不从心，embedding 作为语义空间召回的主力价值会显现。alpha 应该随着系统规模动态调整，所以我把标定工具做成了开源库的一部分。

三态判决不做二选一的强制路由，承认不确定性。CLEAR，Top1 领先明显，直接路由。AMBIGUOUS，Top1 和 Top2 接近，执行 Top1，暗示 Top2，不烦用户追问，在结果末尾附一句如果想看某某也可以告诉我。LOW，全部得分低，降级到闲聊或引导搜索。

单通道降级，LLM 挂了 embedding 仍然工作，embedding 出错 LLM 仍然能推理，两个都挂走 LOW。

融合和参数提取并行进行，不增加额外延迟。

生产环境的成本控制
这里需要专门说一下成本。

双通道融合建好之后，我很快意识到一个问题：ToB 场景里用户表达高度重复。帮张三建个项目、查一下这个迭代进度、哪些任务延期了，这类表达每天出现几十上百次，每次都走一遍 embedding 加 LLM 推理，纯属浪费。

加了三级缓存。

第一级，完全匹配缓存。用户输入跟历史请求完全一致，直接命中，0 毫秒返回，命中率大概 15 到 20 个点。第二级，embedding 近似缓存。用 cosine similarity 找最近的已解析请求，相似度 0.95 以上直接复用，命中率大概 30 到 35 个点。第三级才是 LLM 推理。前两级都 miss 了才走 LLM，而且 LLM 返回的结果也会写回缓存。

加上这套缓存之后，实际走 LLM 的请求占比降到了不到 40%。剩下 60% 要么命中完全匹配缓存，要么 embedding 通道直接给了 clear 判决。

还有一个容易被忽略的点：域预筛选用的关键词匹配就是传统 NLP，正则、模式、高频词。问题分流那步的规则分类器也是。这些技术在整个链路里承担了前置过滤和快速路由的工作，高频表达走规则秒回，低频长尾才上 LLM。不是什么先搭好 LLM 舞台再挂传统 NLP 装饰，而是从一开始就是 waterfall 结构，便宜的规则和 embedding 先跑，不确定的才上贵的 LLM。

最终成绩与剩余问题
逐阶段演进，单模型基线 104 条准确率 80.8%；双通道融合初版 104 条准确率 87.5%，加了 6.7 个点，意图树补上了任务型识别；双通道融合加口语优化，扩展到 140 条准确率 97.1%，加了 9.6 个点，embed_text 覆盖口语加编排检测修复。

数据分析域变化最大，从单模型最差的 47% 干到了满分。单模型轻量 47%，7 对 15；单模型强力 87%，13 对 15；双通道融合扩展集 100%，21 对 21。

140 条扩展回归中剩 4 条失败，全部是含指代词的短句：那个任务排多久需要上下文才知道指什么，把这个任务调个期需要上下文，这个任务不做了先归档需要上下文，那个需求评审了没需要上下文。结论：剩余失败全部属于对话上下文层的职责范围，意图识别层本身已无系统性短板。


为什么这是一个通用结构
写完这个系统之后，我试着从更抽象的角度理解为什么双通道融合有效。

核心原因是：用户输入是不完整、不确定的信息。而不同方法的区别在于如何利用这些信息。纯规则，信息利用率低但确定性高。纯 embedding，利用表层语义但缺乏推理能力。纯 LLM，推理能力强但稳定性差。单一方法都是对输入信息的单视角解释，因此一定存在盲区。

而 Recall 加 Reasoning 加 Ranking 的结构，我理解成：Recall 从不同视角尽可能保留信息，不遗漏可能性；Reasoning 对候选进行语义推理，补全隐含信息；Ranking 在多信号下做一致性决策，选择最优解释。

这个结构的关键不是具体实现，embedding 还是 BM25，LLM 还是规则引擎，而是通过多信号融合最大化信息利用率，同时控制不确定性。

所以它不只适用于意图识别，也适用于其他需要在不完整信息下选最优解释的场景。RAG 文档选择，Recall 候选文档转 Reasoning 判断相关性转 Ranking 排序。Agent 工具选择，Recall 可用工具转 Reasoning 判断可行性转 Ranking 决策。推荐系统，Recall 候选集转 Reasoning 用户意图转 Ranking 精排。

沉淀的设计原则
最后把踩坑过程中最值得带走的几条总结一下。

LLM 只做理解，代码做决策。让 LLM 告诉你用户消息涉及哪些意图，代码决定路由策略。不要让 LLM 同时做 NLU 加路由加编排判断。

不要把 LLM 的 confidence 当硬开关。LLM 置信度是不稳定的主观信号。用它做弱参考，5% 权重，不要用它做路由阈值。用结构化信号，必填参数覆盖率、动词匹配、参数模式，做主决策。

一段描述不可能同时对两个通道最优。embedding 需要关键词密集的同义词集合，LLM 需要描述性强且含消歧提示的自然语言。独立维护 embed_text 和 tree_text。

先判断问题类型，再挑具体意图。操作教学、经营策略、数据查询、执行动作，这些是不同类型的问题，不应该在同一条链路里竞争。

不要低估短句和口语的破坏力。用户不会按你的意图描述说话。调个期、人够不够、最近啥项目赶，这些口语化表达必须在 embed_text 中显式覆盖。

歧义和多意图是两件事。查一下这个任务是歧义，同实体多种查询方式，不是多意图如建项目加拉群。歧义选最优，多意图走编排。

很多识别错误不是识别层的错。可能是缺上下文，分析一下不知道分析谁；可能是缺对话状态管理，好的不知道是接受提议还是新话题。归因到意图识别之前，先检查上下游。

缓存不是锦上添花，是第一天就该设计的东西。ToB 场景里用户表达高度重复，完全匹配缓存的命中率比你想象的高。我后悔没在第一阶段就加上。

单一信号一定有盲区，多信号融合才是出路。embedding 和 LLM 各有擅长和不擅长的场景，没有一个能覆盖全部 case。与其追求单一方案的完美，不如让多个不完美的信号互相补位。

第七阶段：扔掉意图这个词
写完上面这些之后，我遇到了一条让我重新审视整个方案的评论。

原话是：意图是个过时的概念了，不要在 LLM 上用 BERT 时代的东西。

我当时回了一段关于阶段选择的解释，但那几天脑子里一直在转这件事。后来想明白了，他说得对。

我做的这套系统，从第一天起就叫意图识别，但实际上做的事情从头到尾都是工具路由。用户说帮张三建个项目，系统不是要把它分到取消任务这个类别里，而是要决定调哪个 API、传什么参数。这个区别不是咬文嚼字。意图分类和工具路由，在 BERT 时代可以是同一件事，因为在那个范式下你能做的就是把文本映射到预定义标签。但在 LLM 时代，这两个是不同的问题。

意图分类的假设是你有一组固定的类别，每句话必须落进其中一个。这个假设在 LLM 出现之前是合理的，因为模型的能力上限就是分类。但现在 LLM 可以直接理解用户想干什么然后生成对应的函数调用，你不需要中间那个分类层。

所以我把整个系统重新审视了一遍，发现其实不需要改什么架构。

双通道融合里的 embedding 通道，原来召回的是意图候选，现在召回的是工具候选。意图树推理通道，原来 LLM 在意图树上遍历，现在在工具树上遍历。融合评分、三态判决、缓存、waterfall，全部复用。

变的只有一件事：定义方式。

原来定义意图：

IntentEntry(
name="create_task",
domain="项目管理",
embed_text="建任务, 排期, 分配, 创建任务, 帮我建",
tree_text="用户想给某个客户创建任务。区分：查任务→check_task；取消任务→cancel_task",
)

现在定义工具：

ToolEntry(
name="create_task",
description="给指定成员创建任务，需要成员信息和排期",
parameters={
"customer_name": {"type": "string", "description": "客户名称"},
"timeline": {"type": "string", "description": "排期时间"},
},
embed_text="建任务, 排期, 分配, 创建任务, 帮我建",
tree_text="用户想给某个客户创建任务。区分：查任务→check_task；取消任务→cancel_task",
)

多了参数 schema，LLM 不仅能选工具，还能直接填参数。参数提取那一步不用单独跑一次 LLM 了，工具选择的时候顺手把参数带出来。

这个改动的实际收益比想象的大。

第一，加新能力变简单了。原来加一个意图要纠结放哪个域、跟哪些意图语义冲突、embed_text 够不够全。现在加一个工具就是写一段描述加参数 schema，LLM 自己判断适用范围。

第二，长尾表达覆盖更好。原来总有口语化表达匹配不到对应意图，因为你怎么定义都定义不全。现在 LLM 看了工具描述，自己推理这个工具能不能处理这句话。不需要你预判所有说法。

第三，跨域问题自然解决。原来的架构里一个意图只能属于一个域，但现实中有很多跨域表达。比如帮我分析一下这个项目的进度然后给出建议，涉及项目查询、进度分析、任务推荐三个域三个工具。用工具路由的话 LLM 直接返回三个 tool_call，编排层串起来执行，不需要在意图层纠结分类。

关键洞察是：这六个阶段搭起来的架构，双通道融合、三态判决、缓存 waterfall、结构化校验，这些不是意图识别专用架构。它们是通用的选择加路由架构。把被选择的对象从意图换成工具，整个系统零改动就能跑。

所以我在 intent-fusion 里加了一个 ToolRouter 模式。用法几乎一样：

from intent_fusion import ToolRouter, ToolEntry

tools = [
ToolEntry(
name="create_task",
description="给指定成员创建任务",
parameters={...},
embed_text="建任务, 排期, 分配, 帮我建",
tree_text="创建任务。区分：查任务→check_task；取消任务→cancel_task",
),
]

router = ToolRouter(tools)
result = await router.fuse("帮张三建个项目", your_llm)
# result.selected_tool → "cancel_task"
# result.parameters → {"order_id": "xxx"}

回头看，这个演进路径其实是必然的。意图识别是 BERT 时代留下的思维惯性，先分类再执行。LLM 时代应该直接映射到执行。但搭建这套映射系统需要的工程能力，召回、推理、排序、缓存、降级，跟我前面六个阶段积累的东西完全重合。

所以如果你现在从零开始做类似的事，我建议直接上工具路由，跳过意图识别这一步。但如果你已经有一套意图识别系统在跑，迁移成本很低，把意图定义改成工具定义，加参数 schema，其他不动。


投了三十份AI Agent简历，石沉大海。

帮十几个同学改完之后，我发现所有人都踩了同一个坑。

这个坑不是技术栈写得不够多，也不是项目经验不够炫，而是简历里全在描述自己做了什么，没说解决了什么。

面试官扫一眼就pass，不是因为技术不行，而是看不到价值。

我把这个坑讲清楚，顺便说一下AI Agent工程师简历到底怎么写，面试官真正在意的是哪几个点。

先说结论

AI Agent工程师这个岗位，2026年的招聘门槛和2023年已经完全不一样了。

两年前你只要在简历里写熟悉LangChain、了解RAG就能拿到面试。

现在不行了。

我看过上百份AI Agent方向的简历，通过初筛的那些，项目描述有一个共同特点：能用一句话说清楚这个Agent解决了一个什么真实问题，效果提升了百分之多少。

写不出来的，初筛就挂了。

面试官到底在看什么

把阿里、腾讯、字节、微软这几家公司的AI Agent岗位JD拆开来看，其实面试官关注的能力可以归成四个点。

第一个点：技术深度，不是用过而是懂原理

很多人简历里写精通LangChain，面试官一问AgentExecutor的工作原理，答不上来。

这不是精通，这是调用过API。

有区分度的写法是这样的：不止写用了什么框架，还写你理解了这个框架的哪部分机制，基于这个理解做了什么优化。

比如同样是用LangChain，有人写使用了LangChain开发Agent，有人写基于LangChain的AgentExecutor定制错误处理，解决API限流导致的任务中断问题，任务完成率从65%提升到92%。

后者通过初筛的概率，我觉得能差3倍。

代码片段如果是项目里的真实代码，保留，用灰色背景块区分：

from langchain.agents import AgentExecutor

from langchain_core.prompts import ChatPromptTemplate



custom_prompt = ChatPromptTemplate.from_messages([

("system", "You are a helpful assistant. Use tools efficiently."),

("human", "{input}"),

("placeholder", "{agent_scratchpad}")

])



class CustomAgentExecutor(AgentExecutor):

def handleerror(self, error: Exception) -> str:

if "rate limit" in str(error).lower():

return "API rate limit exceeded. Please try again later."

return f"Unexpected error: {str(error)}"

这段代码本身不重要，重要的是你在简历里描述它的方式。

第二个点：工程化能力，从能跑到能上线

这是大多数人的短板。

简历里写使用LangChain开发Agent的人很多，写得出用Docker容器化部署、Prometheus监控P99延迟、K8s HPA自动扩缩容的人很少。

经历过生产环境的人都知道，一个Agent能不能上线，关键不在于大模型调用是否成功，而在于：

限流怎么处理

超时怎么重试

异常怎么降级

Token消耗怎么控制

多用户并发怎么隔离

这些在工程化层面，面试官一看简历就知道你有没有真刀真枪干过。

第三个点：问题解决能力，用Agent解决真实场景

表格对比一下单Agent和多Agent范式在不同场景下的适用性：

面试官问你项目经验的时候，期望听到的不是你用了什么技术，而是你面对的是什么业务问题，为什么选这个方案，效果怎么衡量的。

第四个点：学习与创新潜力

Agent这个方向，技术迭代速度非常快。

三个月前还是LangChain一家独大，现在LangGraph、AutoGen、CrewAI各有优势。

简历里如果能体现出你对不同框架的横向对比和选型思考，会是一个非常强的加分项。

不需要写很长，一句话就够了：

对比LangGraph和AutoGen在多智能体协作场景下的表现，最终选型AutoGen，原因是GroupChat机制更适配动态任务分配需求。

项目经验怎么写才对

这里直接用STAR-L法则，五个字母对应简历里每个项目的写法。

Situation：项目背景，要解决什么问题

Task：你的职责是什么

Action：你采取了什么技术方案

Result：量化结果，最好有百分比或绝对值

Learning：技术反思，一句话带过

反例和正例对比一下。

反例：

使用LangChain开发了一个智能客服Agent，集成了RAG知识库和Function Calling工具调用，实现了自动对话功能。

这段话的问题在哪，读完之后不知道这个项目解决了什么问题，也不知道效果怎么样，更不知道面试官该怎么判断这个候选人的水平。

正例：

针对电商售后场景客服响应慢的问题（日均工单1200，人工平均响应时间15分钟），基于LangChain搭建Multi-Agent系统，将查询订单、退款审核、物流追踪拆分成三个子Agent协作完成。上线后人工介入率从100%降到32%，平均响应时间压缩到45秒。反思：初期未考虑限流降级，高并发场景下部分请求超时，后续引入令牌桶限流解决。

这两段话的信息密度差了不止一个量级。

说一个具体的案例

上个月帮一个同学改简历，他的项目经历里写了这样一段话：

基于AutoGen实现了多智能体协作系统，包含Researcher、Planner、Booker、Reviewer四个角色，使用GPT-4-Turbo作为底层模型，集成了Pinecone向量数据库存储用户偏好。

我问他，这个系统解决了什么问题。

他说是一个旅行规划助手，用户输入需求，系统自动生成行程。

我再问，那和市面上的旅行规划产品有什么区别？你的系统有什么亮点的地方？

他答不上来。

后来我们一起把这段话改成这样：

针对自由行旅行规划中信息分散、迭代效率低的问题，基于AutoGen构建四角色协作系统（Researcher收集信息、Planner制定行程、Booker执行预订、Reviewer优化建议）。设计Docker沙箱隔离工具调用，防止恶意API请求。用户偏好通过Pinecone向量化存储，跨会话生效。系统在高并发场景下引入GPT-4到GPT-3.5的自动降级机制，API成本降低40%。50人内测NPS评分8.2/10，任务完成率92%（单Agent基线65%）。

改完之后两周，他拿到了三个面试邀请。

技术栈怎么写

技术栈是HR筛选的第一道关。

写技术栈有两个常见错误，一个是罗列太多，另一个是只有名词没有上下文。

错误写法：

熟悉LangChain、AutoGen、LangGraph、RAG、Function Calling、Prompt Engineering、Pinecone、Milvus、Docker、K8s、Prometheus、OpenTelemetry。

正确写法（分层展示）：

深度使用：LangChain（3个生产级项目，熟悉AgentExecutor源码）、Pinecone（混合检索，优化召回率22%）、Docker/K8s（Agent服务容器化部署，配置HPA自动扩缩容）。

了解原理：AutoGen（复现多智能体论文，在自建数据集验证协同效果）、vLLM（本地部署Qwen-7B，推理速度提升3倍）。

接触学习：LangGraph（完成官方教程，理解状态机设计模式）、Weaviate（测试混合搜索功能）。

这样写的好处是，面试官一眼就能看出你的真实水平层级，面试的时候提问也有针对性，对双方都高效。

这里插个福利。如果你现在正在找工作，或者想提前准备AI开发方向的求职，需要简历优化、面试辅导、求职陪跑相关的帮助，可以在置顶动态里找到联系方式，找云老师聊聊。很多同学改完简历和面试辅导之后，面试邀约率和通过率提升还是挺明显的。

最后说两句

AI Agent工程师这个岗位，2026年还会继续热门下去。



───────────────────────

故事是这样的。

一个同学上个月来找我，说他做了个内部知识库，接了RAG，跑起来了，演示时候还挺顺，结果一上线就被各种反馈说答案乱套，文档里明明有的内容查不到，查到的又是风马牛不相及的段落。

他当时挺崩溃，觉得是大模型的问题，换了几个不同的LLM，还是一样，然后开始怀疑是不是向量数据库有问题，最后直接把问题甩给了我。

我翻了他的代码，看了大概二十分钟，问他，你文档是怎么切的？

他说按字数，每五百个字一刀。

我当时没说什么，但心里已经知道问题在哪了。

───────────────────────

这两年做大模型应用开发的同学越来越多，RAG几乎是必过的一关。原理听起来很朴素，先检索相关文档片段，再把这些片段塞给LLM当参考资料，让它根据这些资料回答问题，避免大模型凭空乱编。

理论上确实简单。

但实际落地的时候，卡点会接连出现，而且很多问题不是查API文档能解决的，是靠踩坑才能长进的那种。

我见过不少同学拿RAG项目去面试，被问到文档切分策略时直接答不上来，或者说就是用LangChain的默认切分，然后就没了。面试官一般不会当场刁难，但印象分就这样没了。

这篇文章我就来聊聊RAG开发里那几个最容易翻车的地方，以及真正值得写进简历的技术点。

───────────────────────

坑一，文档切分

按字数机械切割，是RAG项目里最普遍的错误。

问题的核心在于，文档的语义边界和字数完全不对齐。你有一份数据库故障处理手册，里面记录了MySQL主从切换的前置检查、执行步骤和回滚方案，这三个部分之间有逻辑关系，但如果你按五百字一刀，可能刚好把前置检查的第三步和执行步骤的第一步切到了两个不同的chunk，用户问一个完整的操作流程，检索回来的两个片段各讲半截，大模型拿这个来回答，答案自然就乱了。

真正靠谱的切分，是要尊重文档自身的结构。如果是Markdown文档，按标题层级来划分是更合理的做法，同时给每个chunk加上来源路径，比如「MySQL主从切换 > 前置检查」，这样既能保留上下文语义，检索回来的片段也更容易让大模型读懂。

class SmartDocumentSplitter:

"""语义感知的文档切分器"""



def init(self, max_chunk_size=800, min_chunk_size=100):

self.max_chunk_size = max_chunk_size

self.min_chunk_size = min_chunk_size



def split_markdown(self, text: str) -> list:

chunks = []

current_headers = {1: '', 2: '', 3: ''}

lines = text.split('\n')

current_content = []



for line in lines:

header_match = re.match(r'^(#{1,3})\s+(.+)$', line)



if header_match:

if current_content:

chunk_text = '\n'.join(current_content).strip()

if len(chunk_text) >= self.min_chunk_size:

chunks.append({

'content': chunk_text,

'context_path': self._build_context_path(current_headers)

})

current_content = []



level = len(header_match.group(1))

title = header_match.group(2)

current_headers[level] = title

current_content.append(line)

else:

current_content.append(line)

if len('\n'.join(current_content)) > self.max_chunk_size:

chunks.append({

'content': '\n'.join(current_content).strip(),

'context_path': self._build_context_path(current_headers)

})

current_content = []



return chunks



def buildcontext_path(self, headers: dict) -> str:

path_parts = [h for h in [headers[1], headers[2], headers[3]] if h]

return ' > '.join(path_parts) if path_parts else '未分类'

这个切分器做了两件事，一是按标题结构来感知文档的边界，二是给每个chunk生成上下文路径。后者很多人会忽略，但它对检索精度的提升很明显。

───────────────────────

坑二，语义鸿沟

文档里写的是「MySQL服务异常」，用户问的是「数据库挂了咋办」。

向量模型会把这两句话各自编码成向量，计算相似度，表面上是语义搜索，但在这种口语和书面语差异很大的场景下，纯向量检索会频繁失准。

靠谱的做法是两阶段检索，先用向量召回二十个候选，再用重排序模型精排出五个。

BGE系列模型在这块做得不错，BAAI/bge-reranker-base在中文语境下效果还可以，重排之后相关性会明显比只用向量检索准。

class EnhancedRetriever:

"""两阶段检索，向量粗筛加重排序精排"""



def retrieve_with_rerank(self, query: str, initial_top_k=20, final_top_k=5):

# 第一阶段，向量检索召回更多候选

initial_results = self.vector_store.search(query, top_k=initial_top_k)



candidates = [{'content': hit.content, 'vector_score': hit.score}

for hit in initial_results]



# 第二阶段，重排序

rerank_scores = self._compute_rerank_scores(

query, [c['content'] for c in candidates]

)



for i, score in enumerate(rerank_scores):

candidates[i]['rerank_score'] = score



candidates.sort(key=lambda x: x['rerank_score'], reverse=True)

return candidates[:final_top_k]

还有个进阶技巧是查询扩展，用大模型把用户的问题改写成三到五个不同表述的变体，分别检索再合并去重，覆盖口语化表达的时候效果会更好。

下面是整体的RAG Pipeline流程：

用户提问

│

▼

┌──────────────┐

│ 意图识别 │ 区分知识库问答/闲聊/代码

└──────┬───────┘

▼

┌──────────────┐

│ 查询扩展 │ LLM改写生成多个变体

└──────┬───────┘

▼

┌──────────────┐

│ 向量检索 │ 召回20个候选

└──────┬───────┘

▼

┌──────────────┐

│ 重排序 │ BGE精排取Top5

└──────┬───────┘

▼

┌──────────────┐

│ Prompt构建 │ 按问题类型动态调整

└──────┬───────┘

▼

┌──────────────┐

│ LLM生成 │ 流式输出

└──────┬───────┘

▼

答案 + 来源引用

这套流程跑通之后，日均查询两百多次，平均响应两秒多，首字节延迟控制在零点八秒左右，用户满意度从最初的一塌糊涂慢慢爬到了七成以上。

───────────────────────

这里插个福利。如果你现在正在找工作，或者想提前准备大模型应用开发方向，需要简历优化、面试辅导、求职陪跑相关的帮助，可以在置顶动态里找到联系方式，找云老师聊聊。很多同学改完简历之后，面试邀约率和通过率提升都挺明显的。

───────────────────────

坑三，Prompt写烂了

大模型回答问题时会编造，这不是RAG的问题，是大模型的底层特性。

但很多人搭完RAG之后，Prompt还是一句话，「请根据以下资料回答用户问题」，然后就把文档片段全粘上去了。大模型当然可以回答，但它没被告知资料里如果没有该怎么办，也没被引导给出信息来源，回答质量就看大模型当天心情。

有效的Prompt需要做三件事，明确告诉模型只使用参考资料里的信息，告诉它没有时要说没有而不是编造，让它标注每句话对应的是哪份资料。

另外更进一步的做法是根据问题类型动态调整Prompt格式。用户在问操作流程和在问概念定义，回答的结构应该是不一样的，操作类问题用分步骤格式，概念类问题先给核心定义再展开，故障排查类问题先列可能原因再给解决方案。

这个优化做完，用户觉得「答案看起来不对」的反馈会减少很多。

───────────────────────

聊聊这些坑在简历上怎么写

这些踩坑经历，很多同学在写简历的时候要么完全不写，要么就写「使用LangChain搭建了RAG系统，实现了智能问答」，这样的描述拿去面试，面试官基本上不会有什么反应。

差异化的写法是把你解决了什么具体问题，用什么技术手段解决的，最终效果如何量化出来，写进项目里。

比如这样，「针对企业内部知识库问答场景，基于BGE Embedding + Milvus向量数据库搭建两阶段检索系统，通过语义分块策略解决文档语义割裂问题，结合重排序模型将检索相关性提升约四十个百分点，系统上线后用户满意度达到七成以上」。

这样的描述有场景，有技术，有数据，面试官看到能直接问你问题，你也能答上来，双向都不尬。

目前2026年AI开发岗位对RAG经验的考察已经不只是「你有没有用过」，而是「你踩过什么坑，你怎么解决的」，这个层次的考察，靠背文档是回答不了的，只有真正下场做过才知道怎么讲。

───────────────────────

这是做大模型应用开发最真实的一件事，走捷径的代价，就是在面试时哑口无言。

真正的积累，是从每一个搜不到的chunk、每一次大模型编造的答案、每一个不好使的Prompt里一点一点攒出来的。



最近和几个做AI开发的朋友聊，他们都卡在同一个问题上。

不是技术不行，是不知道该用哪个框架。

LangGraph、CrewAI、AutoGen、Magnetic-One，更难受的是，框架刚学会，文档又更新了。更更难受的是，面试的时候面试官问你用过什么Agent框架，你支支吾吾说用过LangChain，然后对面点点头，说我们现在用LangGraph，气氛一下就尴尬了。

这篇文章我想聊聊2026年这些框架到底怎么选，顺便说说如果你在找AI开发相关的工作，这块该怎么展示出来。

先说一个让我印象深的数据。

有调研显示，2026年78%的企业已经启动了AI Agent的试点项目，但里面只有14%真正跑到了生产规模。剩下那些为什么死掉的，原因排第一的不是技术难度，而是框架选错了，占了43%。

你想想，快一半的失败是因为选框架这件事。

所以框架不是小事，不是随便跟风选个star最多的就行了。

我把现在市面上主流的8个框架梳理了一遍，分类说说我的感受。

第一类，适合做严肃业务的。

LangGraph是这里面最让我放心的一个。它的核心思路是状态机，你得把每一步的状态显式定义出来，节点之间的跳转逻辑也是你自己写的。这套东西用起来确实繁琐，但繁琐换来的是可控。

在金融审批、医疗诊断这种场景里，一旦某步出了问题，LangGraph可以回滚状态、重试，成功率测试下来能到99%以上。这种确定性，CrewAI这些框架给不了。

我一个朋友在银行做AI，他说他们内部评估了好几个框架，最后选LangGraph，理由只有一条，出了问题我能找到是哪步出的，方便背锅。

听着是玩笑话，但确实是核心需求。

第二类，适合快速出活的。

CrewAI在这里面是效率最高的。你把角色定义好，任务定义好，它自己负责分配和执行。测试数据显示，它平均完成时间195秒，Token消耗在几个框架里是最少的之一。

代码也很清晰，你定义一个researcher，一个writer，然后告诉它各自的任务是什么，依赖关系怎么排，然后crew.kickoff()，就跑起来了。

对于内容生产、市场分析这种流程固定的场景，它确实好用。

我自己用过一段时间，最大的感受是上手门槛低，但调试麻烦，任务依赖关系写乱了会悄悄出错，报错信息还不够清晰。所以如果你打算在简历上写CrewAI，一定要说清楚你用它做了什么、踩过什么坑，这才像真的用过。

第三类，AutoGen，适合想玩涌现智能的。

AutoGen是微软出的，思路跟前两个完全不同。它不是预定义流程，而是让Agent之间自由对话协商，通过对话来涌现出解决方案。

这个思路挺迷人的，感觉像是把一群专家塞进一个会议室，让他们自己讨论出方案。

但问题也很明显，它的Token消耗是这几个框架里最高的，平均要比CrewAI多50%。对话式的机制还容易陷入无限循环，你得手动设max_round不然它可能就一直聊下去了。

我的感受是，AutoGen更适合那种开放性探索任务，比如复杂问题诊断、创意生成。如果你的任务有明确的边界和预期输出，换CrewAI或者LangGraph会更省心。

这里插个话，如果你在找AI开发的工作，简历上写框架经验的时候，有一个特别容易踩的坑，就是只写熟悉LangGraph、使用过CrewAI 这种空泛描述，没有场景，没有结果，面试官看了什么感觉都没有。

更好的写法是，基于LangGraph构建了贷款风控Agent，设计了7步状态机工作流，在测试集上流程成功率达到98%，或者使用CrewAI搭建了内容生产流水线，包含信息采集、写作、校对三个角色，日产出效率提升3倍。

这才是展示经验的方式。你用了什么，解决了什么问题，有没有量化结果。

继续说框架。

第四类是Magnetic-One，适合复杂项目的全局协调。这个框架的思路是设一个Orchestrator作为总调度，下面挂多个专门的子Agent。Orchestrator负责把任务拆解、分配、汇总，子Agent负责各自领域的执行。

这个架构对复杂项目非常合适，比如你要开发一个完整的Web应用，可以让一个Agent做需求分析，一个做架构设计，一个做前端，一个做后端，Orchestrator统一协调进度。

但学习曲线比CrewAI陡，Orchestrator本身的能力也很关键，如果你用的底层模型能力不强，协调效果会大打折扣。

还有两个值得一提的。

DeerFlow 2.0是阿里出的，主打可视化编排，可以拖拽构建工作流。对于不怎么写代码的业务同学参与的场景，它的门槛是最低的。但复杂逻辑还是得写自定义代码节点。

Spring AI是给Java开发者准备的，如果你在做企业级的Java后端服务，想把AI能力集成进来，这个是最顺手的，注解驱动，上手基本没什么成本。

说说我自己的判断。

如果你是刚开始学AI Agent开发，我建议先把LangGraph和CrewAI都跑一遍。这两个覆盖了确定性工作流和角色分工两种主流范式，学完这两个，其他框架看文档基本就能上手。

如果你是在找AI开发的工作，简历上建议选择你真正用过的框架，用一两个讲清楚，比泛泛地列一串强多了。

如果你是在做企业的AI项目选型，不用追求一个框架解决所有问题。很多成熟项目都是混合架构，比如用LangGraph保证数据采集的可靠性，再用CrewAI做内容生成，不同阶段用不同框架，组合起来用。

怎么选框架这件事，本质上是在问清楚你的任务特征，流程是不是确定的，成本敏感不敏感，团队熟悉Python还是Java，需不需要中文技术栈支持。想清楚这几个问题，答案基本就出来了。

这里插一个福利，如果你正在做AI全栈或者AI开发方向的求职准备，包括简历里怎么写Agent项目经验、面试里常被问到的框架选型题怎么回答、AI开发岗的投递策略，可以在我置顶动态里找到联系方式，一对一帮你梳理。

最后说一句。

这几年框架一个接一个出来，很容易产生一种焦虑，感觉你学的总是跟不上最新的。但框架背后的核心逻辑变化不大，状态机也好、角色分工也好、对话协商也好，这些范式理解了，换个框架只是换个语法的事。

真正值钱的不是你会用第几个框架，是你知道什么场景该选哪个，以及你有没有在真实项目里踩过坑。



Agent 开发岗简历怎么写才能拿到面试 云老师分享一下经验

今年三四月份帮不少零基础的同学准备大模型面试，前后看了将近一百份 Agent 开发岗的简历。发现一个通病：很多人的简历，技术栈写得没问题，但核心价值被写丢了。

面试官扫一眼就觉得 pass，不是因为技术不行，而是简历里全是"我做了什么"，没有"我解决了什么"。

下面把最常见的三个坑拆开讲，告诉你反面写法错在哪，正确的写法该怎么组织。

坑一：只写"意图识别 / 任务拆解"，没写怎么解决"模糊 query 跑偏"
反面写法示例：

"实现用户 query 意图识别与对话路径动态规划，支持多任务拆解与并行执行。"

问题在哪？

意图识别现在是个 Agent 都带，关键是你怎么做的——规则匹配还是微调了分类模型？准确率多少？边界场景怎么兜底？这些全没说。面试官只能默认你写了几个 if-else，或者直接把 query 扔给大模型让它自己分，毫无技术深度。

正确写法思路：

基础版（有数据、有闭环）：

针对垂直行业用户 query 语义模糊、意图混合的问题，设计了 6 大类 + 22 子类的意图分类体系，基于微调的轻量级分类模型实现 92.7% 的意图识别准确率；针对低置信度（<70%）query 设计了渐进式多轮澄清机制，兜底规则覆盖 85% 的边界场景，最终将大模型任务执行跑偏率从 41% 降至 8% 以内。

进阶版（有工程决策、有体系化设计）：

针对垂直场景用户多意图嵌套、任务依赖复杂的问题，设计了意图分层路由与 DAG 任务编排机制：先通过轻量级模型做一级意图粗分类，路由至对应业务域后再做细粒度意图拆解，明确任务间的串行 / 并行依赖关系；针对歧义 query 设计了触发式澄清策略，避免大模型无依据瞎猜执行，最终将复杂任务的一次执行成功率从 35% 提升至 89%，单轮对话 token 消耗降低 28%。

面试官加分逻辑：说清了为什么这么做（痛点）、怎么做的（设计决策）、做成了什么样（量化结果），每个点都能被独立追问。面试官一眼就知道，你真的踩过意图识别的坑，不是纸上谈兵只做过 demo。

坑二：只写"支持 Function Call 工具调用"，没写生产级链路怎么搭
反面写法示例：

"通过 Function Call 实现多工具 / 多业务接口调用，支持多场景自动化任务执行。"

问题在哪？

面试官看完第一反应：你就是把 API 文档贴给大模型，让它自己选工具自己填参吧？Agent 工具调用的核心从来不是"能调"，而是稳定、可控、容错。鉴权、限流、重试、降级、异常兜底，这些生产环境经常遇到的问题全没提，面试官只会默认你做的是个简单 demo，没上过线。

正确写法思路：

基础版（有标准化、有容错机制）：

设计了标准化的工具 Skill 封装体系，完成 12 个核心业务接口的工具化改造，精细化定义工具入参 schema 与字段约束规则，将大模型填参错误率从 37% 降至 9%；搭建了工具调用前置校验机制，全覆盖鉴权、频率限制、参数合法性校验，同时设计了参数缺失补全、超时重试、连续失败规则降级的全链路容错策略，工具调用整体可用性达 99.95%。

进阶版（有链路管控、有运维体系）：

针对大模型工具选择混乱、多工具长链路调用不可控的问题，设计了工具分层路由与调用链路管控机制：先通过意图分类锁定工具可用域，缩小大模型工具选择范围，避免跨域乱选；针对长链路工具调用，设计了状态快照与断点续跑机制，同时搭建了调用全链路监控告警体系，线上问题定位耗时从小时级降至分钟级，系统连续无故障运行时长超 180 天。

面试官加分逻辑：这样写会让面试官觉得你不是只会让大模型调 API，而是懂生产级系统的工程化落地。企业招 Agent 工程师，最怕的就是只会做 demo、不懂线上稳定性的人。你把全链路的容错、管控、运维都讲清楚了，直接就和"调包侠"拉开了差距。

坑三：只写"支持多轮对话与个性化交互"，没写上下文爆炸和记忆混乱怎么解
反面写法示例：

"实现多轮对话与上下文记忆管理，支持用户个性化交互体验。"

问题在哪？

全是结果，没有过程。多轮对话是把所有历史全塞进 context 里？还是做了结构化记忆？轮次多了 token 爆了怎么办？用户画像怎么维护？什么信息存长期记忆，什么用完就丢？全没说。面试官只会默认你就是用了框架自带的 Memory，一行核心代码都没改。

正确写法思路：

基础版（有分层设计、有成本优化）：

针对多轮对话全量历史塞入 context 导致的 token 爆炸、记忆混乱问题，设计了分层记忆管理机制：将用户结构化画像、长期偏好作为独立 State 单独维护，每轮对话增量更新；对话上下文仅保留近 3 轮核心交互摘要与关键业务数据，无关信息自动过滤，最终将单轮对话平均 token 消耗降低 47%，长轮次对话（>10 轮）的上下文相关度从 62% 提升至 91%。

进阶版（有体系化设计、有业务结果）：

针对垂直场景用户信息跨轮次分散、个性化需求匹配难的问题，设计了双轨记忆管理体系：① 结构化记忆：独立维护用户画像、业务状态、权限信息等结构化数据，支持跨会话持久化与增量更新；② 非结构化记忆：通过向量数据库存储用户历史关键诉求与解决方案，对话时按需精准召回；同时设计了记忆生命周期管理规则，明确不同信息的留存策略与淘汰机制，上线后用户长轮次对话的任务完成率提升 52%，大模型幻觉发生率下降 68%。

面试官加分逻辑：记忆管理是 Agent 落地最核心的痛点之一，绝不是框架自带的简单 Memory 就能解决的。你能讲清自己的设计决策、分层逻辑和业务结果，那么离你的目标岗位也就更近了一步。

最后总结
Agent 开发的简历，从来不是功能的堆砌，而是工程判断力的集中体现。

好的简历，不是让面试官看完无话可说，而是让面试官看完，每一行都想追问，立刻想拿起电话约你面试。





收录于文集
共26篇
云老师分享一下企业内部的记忆系统方案

整个记忆系统分成六个核心环节，从用户输入开始，到记忆被存储、维护、检索，形成一个完整的闭环系统。以法律AI场景为例

环节一：重模型判断"要不要记"

核心机制

用户输入后，系统首先调用一个较强的模型（与主模型同级别或稍小于主模型但大于8B）进行初步筛选，判断信息是否值得记忆。

三维度评分标准

重模型从以下三个维度对每条信息进行评分（1-5分），加权平均后超过阈值（如3.5分）则标记为需要记忆：

持久性价值

判断信息在未来是否有长期使用价值，需要理解业务逻辑和法律领域知识。

法律AI行业具体判断标准：

五维度评分体系：

法律规范性：内容是否涉及明确的法律条文、司法解释、行政法规

判例/案例的时效性与权威性：判例的判决年份、法院层级、是否被推翻

法律关系复杂性：内容描述的法律关系复杂程度

用户重复提及频率：同一类型记忆在不同会话中被反复提及的次数

对未来法律推理的预期贡献：缺少该记忆是否会影响未来类似场景的处理

简化三必过条件：

包含具体法律实体（法条编号、案号、当事人名称）或法律关系

不是一次性的事务性提示

用户明确表达了倾向、原则或重要事实

2. 结构化程度

判断内容是实体关系还是闲聊，相对容易但边界有时模糊（如带法律术语的闲聊）

3. 个性化价值

判断是否反映用户偏好或特征

执行流程

重模型输出YES/NO或分数阈值

算力消耗较大，但实际进入下一环节的比例可能不到20%，总体可控

如果重模型判断为NO，信息直接丢弃；如果为YES，则进入下一环节

环节二：轻量模型做提取和分类

核心机制

确定需要记忆后，系统调用8B左右的小模型进行具体的信息处理。

主要任务

实体提取：从原文中抽取出关键实体

关系识别：识别实体之间的关系

关键句提取：提取核心内容句子

分类标记：按照结构化程度和个性化价值进行分类

输出格式

结构化的记忆条目，例如：

环节三：暂存区去重 + 批量合并

核心机制

提取出的结构化记忆先进入暂存区，进行去重和批量合并处理。

优势分析

避免重复写入存储：防止相同或高度相似的记忆重复写入多个数据库

减少后续处理负担：提前去重减轻后续压缩和总结的工作量

简化并发控制：暂存区可设计为单线程处理，避免复杂的并发控制

提高处理效率：批量合并后一次性写入存储，效率更高

潜在问题及解决方案

问题1：跨窗口重复

现象：不同时段的相同内容可能被重复记录

解决方案：定期在存储层面做全局扫描，或在重要场景写入时增加查询验证

问题2：去重粒度把握

现象：语义相似但表述不同的内容如何处理

解决方案：使用轻量模型计算语义相似度，设定合理阈值（如0.9），或仅做精确去重

问题3：重要差异误删

现象：相似但包含重要差异的信息可能被误删

解决方案：区分"重复"和"更新"，对实体属性类记忆采用更新覆盖或版本链机制

问题4：暂存区性能瓶颈

现象：暂存区积压过多记忆导致处理变慢

解决方案：限制暂存区最大条目数或时间窗口（如最多100条或30秒）

环节四：异步写入存储

核心机制

系统在空闲时异步将暂存区的记忆批量写入正式存储。

执行流程

系统判断主模型是否空闲

空闲时处理暂存区记忆

写入前先查询缓存，避免重复写入

异步写入不阻塞主任务执行

环节五：分层存储架构

三库协同架构

图数据库（Neo4j）：存储法律概念、法条、判例、当事人之间的关联关系

向量库：存储原文或段落的embedding，用于语义检索

关系型数据库：存储结构化元数据（时间戳、访问次数、冷热标记等）

架构优势

关系清晰：不同类型的存储各司其职

检索高效：根据查询需求选择最优存储方式

扩展性强：各层可独立扩展和优化

环节六：动态维护机制

冷热分级策略

核心记忆：重要法律条文、关键判例、用户核心偏好等永久保存

次要记忆：时效性较强或重要性较低的信息设置衰减机制

定期总结优化

Reflection模型扫描：定期扫描新增记忆

智能合并：自动识别并合并相似记忆

矛盾处理：识别并处理相互矛盾的信息

结果回写：将优化结果写回存储层

额外模块：系统控制与引导

Runtime状态层

用于追踪会话状态、任务进度、上下文信息、处理产物、记忆编排和错误处理，作为整个系统的控制面板。

Prompt引导机制

第一步：场景识别

识别Agent在典型任务中需要record和retrieve的场景：

Record场景：

用户明确要求记忆（"你记住这个"、"记一下"）

用户提供新的案件事实、合同条款、当事人信息

Agent完成重要推理步骤，结论值得保留

用户纠正Agent之前的错误理解

Retrieve场景：

用户询问历史信息（"之前那个合同"、"上次说的"）

新任务需要历史背景信息

用户提及之前讨论过的术语或概念

用户要求参考之前结论继续推进

第二步：触发条件设计

为每个场景设计判断原则而非死规则：

Record原则：

包含新的事实、偏好或对之前信息的修正

对未来任务可能有用而非一次性指令

信息来源可靠（用户直接陈述而非模型猜测）

Retrieve原则：

问题包含指向历史对话的关键词

当前任务需要用户之前提供过的信息

缺少回答所需的背景信息且可能已存在

用户要求基于之前结论继续推进

第三步：双层Prompt结构

系统指令：会话开始时设定通用行为准则

动态提示：在特定节点主动询问模型是否需要record/retrieve

第四步：具体Prompt模板

Record Prompt模板：

Retrieve Prompt模板：

第五步：测试与迭代

使用真实对话样例进行测试，识别bad case并持续优化prompt内容。

轻量化实施方案

初期可采用强制触发机制：

用户每发送消息，系统自动调用retrieve

Agent输出最终答案前，系统自动对比并强制调用record

总结

本方案通过六个环节的协同工作，构建了一个高效、智能的Agent记忆系统，特别针对法律AI场景进行了深度优化。系统在保证记忆质量的同时，通过分层架构和异步处理机制，有效控制了性能开销，为构建专业化的法律AI助手提供了坚实的基础。



收录于文集
共26篇
前段时间面了一个男生，聊完智能体之后，我第一反应是：这个人真的吃透了。

现在不少人聊 Agent，还停留在"能调 API 的聊天机器人"这个层面。但他不一样，他已经能完整搭出一个自主系统的骨架，而且知道每一块该怎么设计、为什么这样设计。

面试对话实录
我问他："如果要你设计一个智能旅行规划 Agent，你会从哪些层面去想？不要只列功能。"

他没急着报技术栈，而是先回到根本："第一步，我得先搞清楚这个 Agent 的自主性边界在哪。它是那种全程自己拍板的规划师，还是每一步都要跟用户确认的助手？这个定位直接决定了任务要拆多细、哪些节点必须停下来等人回复。"

我追问："具体怎么搭？"

他说他会按分层架构来设计，四层结构：

规划层：Agent 怎么思考
任务分解的逻辑是关键。用 ReAct 模式，让 Agent 自己一步步推理；还是用 CoT 提前把关键步骤定死？前者灵活但不可控，后者稳定但死板。选哪个，取决于你想要 Agent "活思考" 还是 "死执行"。

工具层：API 之外是编排策略
接航班、酒店 API 只是基础。真正难的是编排策略：查不到直飞时，是自动改查中转方案，还是立刻停下来问用户？这种决策流必须提前预设，不能等出问题了现想。

记忆层：工作记忆 vs 长期记忆
得把两种记忆分开。工作记忆管当前对话的上下文，长期记忆存用户的偏好和习惯。长期记忆用向量数据库来存，规划的时候主动检索，这样 Agent 才能真的"认识"用户，而不是每次都从零开始。

状态与评估层：卡住时怎么办

Agent 必须能随时汇报自己走到哪一步了。更重要的是，卡住了不能沉默或者崩溃，得有预设的降级方案。比如某个 API 挂了，是换备用源，还是直接告诉用户当前进度并建议人工介入？

我继续问："你觉得这跟设计传统产品，那种流程固定的产品，本质区别在哪？"

他答得很干脆："本质区别是对不确定性的处理方式。传统产品像铺铁轨，流程是死的，用户只能按轨道走。Agent 是在一片没地图的地形里自己导航，我的工作不是帮它把路全铺好，而是给它装个靠谱的导航系统、配齐工具箱、再定好几条应急原则——遇到断崖怎么绕，碰上暴雨停不停。"

他的回答里，藏着几个关键认知
系统架构思维：不是单点堆功能，而是从规划、工具、记忆、状态四个模块系统性地搭 Agent。缺一块，整个系统就瘸腿。

对不确定性的管理：核心不是让 Agent 永远不出错，而是出错时有兜底的策略。鲁棒性比完美执行更重要。

记忆是底座，不是装饰：很多人把记忆当锦上添花，但他把记忆当成个性化和持续对话的根基。没有记忆，Agent 永远是个陌生人。

产品经理的角色变了：以前产品经理画的是确定性的流程图，现在定义的是自主系统的运行规则。从"铺路"变成"定规则"。

想达到这种理解深度，可以怎么走
啃框架文档：ReAct、Plan-and-Execute 这些模式不能只看概念，得去翻 LangChain / LangGraph 的官方文档，看它们是怎么落地的。

泡社区看案例：CrewAI、AutoGen 这些项目的社区里有很多真实的多智能体案例，比看十篇论文都管用。

读经典 + 跟前沿：《人工智能：一种现代方法》里智能体相关章节打底，同时跟进最新的论文和行业实践，保持更新。

最后想说的是，核心竞争力是能不能想清楚一个系统在什么情况下该自主、什么情况下该求助、以及怎么在两者之间无缝切换。


云老师分享一下经验

这篇文章把 AI Agent 从接收用户请求到返回最终答案的完整流程拆开讲清楚。不搞概念堆砌，直接看每个环节在干什么、怎么配合。

一、整体流程概览
一句话总结：用户提问 → Agent 调取记忆 → LLM 拆解任务并生成计划 → Agent 判断是否需要工具 → Skill 匹配并准备参数 → LLM 补全参数 → MCP 发现并调用工具 → 工具执行任务并返回 → Skill 校验结果 → Agent 整合上下文 → LLM 生成最终回答 → Agent 返回给用户。

整个链路涉及 5 个核心角色：

用户侧：发起请求的人

智能体（Agent）侧：调度中枢，负责协调全流程

Agent Skills 侧：能力模块，负责工具调用的策略和参数处理

大模型（LLM）侧：大脑，负责理解意图、拆解任务、生成回答

MCP + 工具侧：执行层，负责实际调用外部 API 并返回结果

二、各角色详细拆解
1. 用户侧
用户提问（Query）：用户发起请求，输入问题或指令。这是整个流程的起点，所有后续动作都围绕这个 Query 展开。

2. 智能体（Agent）侧 — 调度中枢
Agent 接收请求：智能体接收用户输入的请求，开始处理流程。

历史对话 / 记忆检索：Agent 调取历史对话上下文与用户记忆，为后续处理提供背景信息。没有记忆的 Agent 就像每次见面都失忆的人，回答质量会大打折扣。

Agent 接收执行计划：接收大模型生成的执行计划，明确接下来要做什么。

判断是否需要调用 Skills / 工具：Agent 判断当前任务是否需要借助外部能力完成。简单问题直接回答，复杂问题才走工具调用链路。

Agent 通过 Skill 发起调用请求：通过 Agent Skills 向 MCP 发起工具调用请求，把任务交给下层执行。

Agent 整合 Skill 结果与上下文：汇总工具返回的结果，结合上下文进行整合处理，为最终回答做准备。

Agent 向用户返回结果：将最终回答整理后，反馈给用户。流程闭环。

3. Agent Skills 侧 — 能力模块
路由到匹配 Skill：根据任务类型，匹配对应的 Agent Skill 能力模块。比如查天气走天气 Skill，查数据库走数据库 Skill。

Skill 读取上下文与能力配置：读取当前任务上下文信息与自身的能力配置参数，知道该怎么调。

Skill 组织调用策略与参数草案：基于任务需求，生成工具调用的策略与参数草稿。这一步是"准备阶段"，还没真正调用。

Skill 解析 / 校验 / 格式化结果：对工具返回的原始结果进行解析、校验，格式化为统一格式。防止脏数据直接传给用户。

4. 大模型（LLM）侧 — 大脑
LLM 意图识别与任务拆解：识别用户请求的核心意图，并将复杂任务拆解为可执行的子任务。比如"帮我整理周报"会被拆成：查数据 → 汇总 → 格式化 → 输出。

生成执行计划：根据拆解后的任务，生成完整的执行步骤与计划。Agent 按照这个计划一步步执行。

LLM 补全具体调用参数：基于 Skill 生成的参数草案，补全、优化工具调用的具体参数。确保传出去的参数是对的。

LLM 生成最终回答：基于整合后的工具结果与上下文，生成面向用户的自然语言回答。这是用户最终看到的内容。

5. MCP + 工具侧 — 执行层
MCP 工具发现与选择：根据 Agent 的调用请求，发现并匹配可用的工具。MCP 是工具的中转站，知道有哪些工具可以用。

MCP 调用具体工具 API：向目标工具发起 API 调用请求，传递执行参数。这一步是真正"动手"的时候。

MCP 接收工具执行结果：接收工具返回的执行结果，并传递给 Agent Skills 进行后续处理。

工具执行具体任务：接收 API 调用请求，执行对应的具体任务，返回执行结果。这是最底层的能力提供者。

三、流程闭环：完整链路串起来
把上面各角色的动作串成一条线，就是 AI Agent 的完整工作流：

四、关键设计要点
记忆机制不能少：没有历史对话和记忆，Agent 每次回答都像第一次聊天，体验很差。记忆是 Agent 智能化的基础。

意图识别是分水岭：LLM 能不能准确理解用户想干什么，决定了后续拆解任务和调用工具的方向对不对。意图识别错了，后面全错。

Skill 是能力封装：每个 Skill 对应一类外部能力（查天气、查数据库、发邮件），Skill 负责把通用请求翻译成具体工具的调用参数。

MCP 是工具中间层：MCP 统一了工具的接入方式，Agent 不需要关心每个工具的具体实现，只需要通过 MCP 发起调用。

校验环节不能省：Skill 对工具返回结果的解析和校验，是防止脏数据、错误数据直接暴露给用户的关键防线。

LLM 负责"动脑"，工具负责"动手"：LLM 做理解、规划、生成；工具做实际的数据查询、文件操作、API 调用。两者分工明确，才能各司其职。

五、一句话总结
AI Agent 的本质是一个"调度中枢 + 大脑 + 执行层"的分层协作系统：Agent 负责协调，LLM 负责理解和生成，Skills 负责能力封装，MCP 和工具负责实际执行。各层之间通过标准化的接口和参数传递协作，最终把用户的自然语言请求，翻译成可执行的动作，再把执行结果翻译回自然语言返回给用户。



云老师分享一下经验

这篇文章把我在实际项目里踩过的坑、验证过的技术方案，一次性讲清楚。不绕概念，直接上干货。

一、企业里最常见的 4 类 AI Agent 项目
以下全是真实在用的场景，没有为了讲而讲的虚例子：

1. 企业内部知识库问答（需求量最大）
实际场景：公司手册、开发文档、规章制度、产品 PDF、面试题库、内部 Wiki……员工直接问：

"跨域怎么解决？"

"公司报销流程是什么？"

核心诉求：不能让大模型瞎编，只能回答公司内部资料里有的内容。答不上来就说不知道，比胡说强。

2. 工具调用办公 Agent
实际场景：让 AI 真的去干活，而不是只聊天：

查天气、查实时新闻

读写本地文档、操作文件

查数据库、调接口取数据

发邮件、整理 Excel

核心诉求：AI 能调用外部工具，把任务闭环完成。

3. 任务拆解自动化 Agent（偏复杂）
实际场景：你给一个大目标，AI 自己拆步骤执行：

"帮我整理一份周报，查 3 个面试高频题+答案，汇总成 Markdown 文件"

AI 自己规划：

确定要查哪些题

联网检索资料

整理内容

生成 Markdown

保存到本地

核心诉求：能自主思考、分步执行、循环试错、最后复盘。

4. 多智能体 Multi-Agent（大厂在用）
实际场景：多个 AI 分工协作：

调研 Agent：负责查资料

写作 Agent：负责写文案

审核 Agent：负责检查对错

执行 Agent：负责跑代码/存文件

核心诉求：单人 AI 搞不定的复杂任务，拆给多个角色协作完成。

二、所有 AI Agent 的通用流程（不管简单复杂都一样）
这个流程是固定的，理解了它，再看任何 Agent 项目都能一眼看懂：

三、当前主流技术栈（个人版 vs 企业版）
基础底层（必选）
RAG 知识库技术栈
Agent 流程编排框架（核心）
LangChain：老框架，简单 Agent、简单工具调用，现在逐步淘汰

LangGraph：现在企业标配，做循环、判断、多步骤、复杂任务

当前行业黄金组合：LlamaIndex + LangGraph

LlamaIndex：管文档、RAG、知识库、切片、检索

LangGraph：管 AI 思考、步骤拆解、循环、工具调度、多 Agent

工具调用层
内置工具：搜索、文件读写、代码解释器、数据库查询

自定义工具：自己写接口、对接业务系统、对接内部平台

四、关键技术优缺点 + 谁在用
1. LlamaIndex
优点：

文档解析极强，PDF/MD/表格兼容性很好

高级语义切片，不会乱断句，RAG 精度高

检索算法强：混合检索、重排序、多路召回

搭知识库很快，基本开箱即用

缺点：

复杂流程、循环逻辑不好写

纯 Agent 调度能力偏弱

谁在用：所有要做企业知识库、私有文档问答的项目

2. LangGraph
优点：

支持分支、循环、判断、反思、重试

可以画任务流程图，可控性极强

多 Agent 分工协作的唯一主流方案

目前大厂复杂 Agent 项目的垄断级选择

缺点：

学习成本高

代码比 LangChain 复杂

谁在用：只要是能自动干活、多步骤任务、复杂 AI，基本都用它

3. LangChain（老框架）
优点：

生态极大，例子多，入门简单

工具接入全

缺点：

默认切片低级粗暴，RAG 效果差

复杂任务写不动，没有原生循环

架构老旧，新项目逐步弃用

谁在用：老项目、老旧教程、简单 Demo，新公司基本不选

4. 向量数据库三家对比
五、两套可以直接抄的技术方案
方案 A：个人开发 / 自学 / 小项目（省钱简单）
适用：自己写 Demo、本地知识库、小工具

方案 B：企业线上正式项目（当前主流）
适用：公司知识库、自动化办公、企业级 AI 系统

六、一句话串起整体
你有一堆公司资料或个人文档，先用 LlamaIndex 做解析和高级切片，再通过 BGE-M3 把切片转成向量，存进 Qdrant/Milvus 向量库——这就是你的 RAG 知识库。当用户提问时，Agent 用 LangGraph 编排流程：检索知识库、调用工具、整合信息、输出答案。

整个链路跑通之后，AI 就能基于你的私有资料，靠谱地回答问题了。



云老师分享一下一些简历的项目，供大家参考
随着AI Agent与AI辅助开发工具（如Claude Code、Cursor、Windsurf）的快速普及，浏览器自动化已成为AI Agent与真实Web应用交互的核心能力。然而，传统的浏览器自动化工具（如Playwright、Selenium）在面对现代Web应用的复杂场景时，暴露出了诸多可靠性瓶颈：动态渲染、Shadow DOM、懒加载导致CSS Selector频繁失效；登录态维护成本高，无法复用用户已有会话；AI模型调用不稳定导致任务中断；异常场景依赖人工处理，维护成本高昂。

为解决这些痛点，开发了Browser Debugging Daemon（BDD）——一款面向AI Agent的浏览器自动化基础设施，作为MCP Server无缝集成主流AI IDE，为AI Agent提供稳定、可靠、高效的浏览器自动化能力，彻底解决传统工具在动态Web应用中的可靠性难题。

项目定位与核心价值
BDD的核心定位是为AI Agent提供高可靠的浏览器自动化底层能力，通过标准化的MCP协议接入，成为Claude Code、Cursor、Windsurf等主流AI IDE的自动化能力底座。其核心价值在于：
- 解决传统自动化工具在动态Web场景下的可靠性问题，大幅提升AI Agent与Web应用交互的成功率；
- 降低AI Agent自动化任务的人工维护成本，实现异常场景的半自动化处理；
- 提供灵活的运行模式与环境适配能力，满足隔离测试、生产环境复用、智能选择等多场景需求。

核心架构设计：Planner-SubAgent分层架构
为实现高内聚、低耦合的设计，BDD采用了Planner-SubAgent分层架构，将任务规划与执行解耦，兼顾灵活性与可靠性：
- Planner（规划层）：负责任务的整体规划、决策与流程控制，基于AI模型理解用户指令，拆解为可执行的浏览器操作序列，并根据任务状态动态调整执行策略。
- BrowserSubAgent（执行层）：通过Chrome DevTools Protocol（CDP）直接与浏览器实例通信，执行具体的浏览器操作，如元素点击、页面导航、数据提取等，同时负责底层异常的捕获与上报。

为适配不同场景的需求，BDD设计了三种运行模式：

关键技术突破与优化
1. 动态元素定位系统：解决Selector失效难题
现代Web应用广泛使用Shadow DOM、懒加载、动态渲染等技术，导致传统基于CSS Selector的定位方式成功率不足60%，严重影响自动化任务的稳定性。

针对这一痛点，BDD设计了Set-of-Mark 视觉定位算法，通过元素特征指纹化实现跨渲染周期的元素追踪：
- 多维度特征融合：结合标签类型（权重40%）、ARIA角色（权重15%）、文本相似度（权重×60）与空间距离衰减（-20/25px）构建元素特征指纹；
- 动态匹配机制：在DOM结构变化后，通过特征指纹重新匹配目标元素，实现跨渲染周期的稳定追踪。

优化效果：DOM变化后重匹配成功率达92%，整体元素定位成功率提升至95%+，彻底解决了动态Web场景下的元素定位失效问题。

2. 人机协作状态机：实现异常场景的半自动化处理
自动化任务在执行过程中不可避免会遇到验证码、弹窗、异常页面等场景，传统工具缺乏有效的异常处理机制，往往需要人工介入，维护成本极高。

BDD设计了6状态任务生命周期状态机（queued→running→waiting→manual→completed/failed），实现了AI自主处理与人工接管的无缝衔接：
- 异常自主处理：针对常见异常（如弹窗、页面加载超时），AI Agent可自主识别并处理，成功率超过90%；
- 断点恢复机制：当遇到无法自主处理的场景时，系统自动暂停任务并保存完整上下文（截图、DOM快照、操作历史），人工接管后可快速恢复执行；
- 效率提升：单次任务人工干预时间从传统的15分钟降至2分钟以内，人工值守成本降低90%。

3. Attached 模式：消除环境准备瓶颈
传统自动化工具（如Playwright）每次执行任务都需要启动全新的浏览器实例，并重新完成登录流程，平均耗时5分钟，且需要维护大量账号池来支撑多场景自动化，成本高昂。

BDD的Attached模式通过Chrome DevTools Protocol直接attach到用户已登录的浏览器实例，复用用户的Cookie、LocalStorage等登录态，实现了：
- 测试用例准备时间降至0，无需启动新浏览器或重新登录；
- 支持在已登录的业务系统中直接执行自动化任务，无需维护账号池，大幅降低环境准备成本。

4. 模型容错与降级：保障AI驱动任务的稳定性
AI Planner依赖大模型进行任务规划，而模型服务的不可用、超时或错误会直接导致任务失败。BDD实现了OpenAI Planner的模型自动降级策略：
- 当主模型（如GPT-5.4）不可用时，系统可在200ms内自动降级到fallback列表中的其他模型；
- 单次请求失败率从8%降至1%以下，任务整体成功率提升至99%，保障了自动化任务的稳定执行。


同时，BDD已作为MCP Server被Claude Code、Cursor、Windsurf等主流AI IDE集成，为AI Agent提供稳定的浏览器自动化能力支撑。
总结与展望
Browser Debugging Daemon通过分层架构设计、视觉定位算法、人机协作状态机、Attached运行模式与模型容错降级策略，系统性解决了传统浏览器自动化工具在AI Agent场景下的可靠性痛点，大幅提升了自动化任务的成功率与执行效率，降低了人工维护成本。

未来，我将继续优化BDD的能力：进一步提升复杂场景下的元素定位精度，支持更多浏览器内核；拓展多标签页、多窗口的自动化能力；优化AI Planner的任务拆解逻辑，实现更复杂的端到端自动化流程，为AI Agent提供更强大、更可靠的浏览器交互能力。



模块一：AI专业词汇（2h）
核心目标：建立术语体系，看懂文档和面试题。

模块二：Python基础 + CRUD（5h）
一句话理解：给AI做外卖窗口：前端用户点单，后端AI做菜，你需要一个传菜窗口。

FastAPI：现代高性能Web框架，比Flask快、比Django轻，自动生成Swagger文档。核心：异步(async/await)、Pydantic自动校验、Uvicorn部署。

CRUD：增删改查。用FastAPI写REST接口，SQLAlchemy ORM操作数据库（不用手写SQL），Pydantic做参数校验（像门口保安）。

必会代码模式：@app.post("/ask/") 创建 | @app.get("/history/{id}") 读取 | @app.put("/answers/{id}") 更新 | @app.delete("/answers/{id}") 删除

面试重点：async/await作用、ORM的Session(commit/rollback)、Pydantic数据校验

模块三：LangChain（4h）
一句话理解：胶水框架，把AI模型、数据、外部工具粘成自动流水线。像搭乐高，用 | 管道符拼接。

PromptTemplate：填空题模板。把用户问题填进固定格式再送给AI。

Chain / LCEL：链式调用：prompt | llm | parser。上一步输出自动作为下一步输入。LCEL是新写法，支持流式、并行、重试。

OutputParser：把AI啰嗦的回答修剪成你要的格式（JSON、列表）。

Memory：让AI记得之前聊过什么。Buffer（全记）/ Summary（记摘要）/ Vector（记相似历史）。

Tools：给AI手脚：查天气、搜百度、算数学、调数据库。用@tool装饰器定义。

Agent / ReAct：给AI大脑，自己决定用什么工具。循环：思考(Thought)→行动(Action)→观察(Observation)→最终回答(Final Answer)。

实战路径：①简单问答链 → ②RAG知识库链 → ③Agent+工具 → ④Multi-Agent协作

模块四：LlamaIndex（4h）
一句话理解：专门做AI查资料的框架，比LangChain的RAG更专业、更省事。LangChain像瑞士军刀，LlamaIndex像专业订书机。

Node：文档切分后的知识卡片，带元数据（来源文件、页码）。

Index：编目系统。VectorStoreIndex（按内容主题，最常用）/ SummaryIndex（按摘要）/ TreeIndex（树形目录）/ KeywordTableIndex（关键词）。

Retriever：图书管理员。VectorRetriever（语义）/ BM25Retriever（关键词）/ Hybrid（混合）。

Reranker：精筛专家。粗筛100条后，用复杂模型挑出Top 5，准确率提升30%+。

Query Engine：一键问答机器人。load_data → VectorStoreIndex → as_query_engine → query("问题")。

评估体系：Faithfulness（答案能否在原文找到依据，防幻觉）/ Relevancy（检索内容跟问题相关吗）。企业上线前必跑。

高级特性：GraphRAG（知识图谱+向量，能做跨文档推理）/ 增量更新（新文档来了只更新新增部分）。

模块五：RAG检索增强生成（4h）★ 最核心
一句话理解：让AI开卷考试的完整方案。企业落地AI的头号选择，比重新训练便宜10000倍，知识更新只需换本书。

① 文档加载：PDF/Word/Excel→纯文本。PyPDFLoader（简单PDF）、PDFplumber（带表格）、Unstructured（万能）。

② 文本分块：chunk_size（每块多大，通常256-512 token）、chunk_overlap（重叠量，防断句）。策略：递归字符切分（先段落→再句子→再字，最推荐）> 固定长度切 > 语义切分。

③ 向量化：把文字变成数字向量。中文首选BAAI/bge-large-zh-v1.5（开源免费），英文用OpenAI text-embedding-3-large。

④ 向量存储：FAISS（纯内存，本地Demo）、Chroma（自动持久化，小项目）、Milvus（分布式，企业生产）。检索原理：ANN近似最近邻算法。

⑤ 检索生成：问题向量化→向量库找Top K→拼接Prompt（参考资料+问题+约束）→LLM生成答案。关键技术：Rerank精排、Hybrid混合检索（BM25+向量）。

进阶玩法：Self-RAG（生成后自我验证，错了重做）/ GraphRAG（知识图谱+向量，跨文档推理）/ Agentic RAG（Agent动态决定检索策略）。

模块六：向量数据库 Milvus（4h）
一句话理解：企业级语义搜索引擎。传统数据库像查身份证号（精确匹配），Milvus像以图搜图（找相似的）。

核心概念：Collection（超级表，存向量+普通字段）/ Partition（分区，查的时候只翻相关书架）/ Index（向量目录）。

索引类型：HNSW（最常用，建导航图，查询极快，适合生产）/ IVF_FLAT（先聚类再桶内搜索，平衡型）/ FLAT（暴力搜索，100%准但极慢，<1万条用）。

相似度度量：COSINE（余弦相似度，最通用，不受向量长度影响）/ L2（欧氏距离）/ IP（内积，需归一化）。面试问选哪个→COSINE。

Python SDK核心：connect连接 → 定义FieldSchema（id/text/vector）→ Collection建表 → insert插入 → create_index建索引 → search检索。

选型对比：FAISS=玩具车（纯内存，无持久化）/ Chroma=私家车（小项目）/ Milvus=大巴车（分布式、十亿级、混合检索）。

模块七：AutoGen（4h）
一句话理解：微软开源的多Agent协作框架。不再一个AI单打独斗，而是多个AI角色像公司一样分工合作。

ConversableAgent：所有Agent的基类，能说话、收消息、回复。

UserProxyAgent：代表人类的Agent。能执行代码（本地或Docker），把结果反馈给其他AI。像你的私人助理。

AssistantAgent：专门动脑的AI，负责写代码、写文档、做分析。像公司技术骨干。

GroupChat：多Agent群聊。Round Robin（轮流发言）/ Auto（Manager决定谁接话）。

Code Execution：AutoGen最大特色：AI写的代码能真跑起来，在Docker沙盒里执行，安全隔离。

vs LangChain Agent：LangChain是单Agent+工具（一个全能员工）；AutoGen是多Agent协作（一个项目组），更适合复杂长流程任务。

模块八：项目实战 + 面试题
必做项目：个人/企业知识库助手

技术栈：FastAPI + LlamaIndex/LangChain + Milvus + BGE Embedding

完整流程：前端上传PDF → FastAPI接收 → PDF解析 → 递归分块 → BGE向量化 → Milvus存储 → 用户提问 → 向量检索 → Rerank精排 → 拼接Prompt → LLM生成 → 返回答案+出处

加分项：多轮对话Memory、Faithfulness评估、答案溯源（显示出自哪页）、混合检索

进阶项目：智能数据分析（自然语言→SQL→图表，LangChain Agent + SQLDatabase工具）/ Multi-Agent客服（意图识别→检索→生成→审核，AutoGen GroupChat）。

高频面试题 & 标准答法

Q1：RAG和Fine-tuning的区别？

A：RAG是开卷考试，适合知识常更新、数据量小，成本低可溯源；Fine-tuning是重新上课，适合需改模型行为或数据量大，成本高。可结合：微调提升基础能力，RAG补实时知识。

Q2：怎么解决RAG幻觉？

A：三层防护：①检索层用Reranker提精度；②Prompt约束"只基于资料回答，不知道就说不知道"；③生成后用Faithfulness评估验证。

Q3：chunk_size多大合适？

A：无银弹。通常256-512 token，overlap 10%-20%。结构清晰按标题切；连续文本用RecursiveCharacterTextSplitter。

Q4：Embedding模型怎么选？

A：中文首选BAAI/bge-large-zh-v1.5（开源稳定），英文用text-embedding-3-large。看MTEB榜单。

Q5：Milvus HNSW vs IVF_FLAT？

A：<100万且要100%准→FLAT；数据量大要速度→HNSW（建时慢查时快）；平衡→IVF_FLAT。企业生产默认HNSW。

Q6：LangChain LCEL优势？

A：管道符|拼接，代码简洁；原生支持流式、并行、重试；每步都是Runnable可单独测试。传统Chain是面向对象，扩展性弱。

Q7：Agent的ReAct模式？

A：Reason+Act。循环：Thought思考→Action调用工具→Observation观察结果→直到足够信息→Final Answer。让模型从一次性回答变成多步推理。

进阶方向
4周极速学习路线
第1周：AI词汇（2h背熟）→ Python+FastAPI（3天，跑通接口）→ LangChain基础（3天，手敲Chain）

第2周：RAG五步法（3天，理解每步原理+手写代码）→ LlamaIndex（2天，PDF问答Demo）→ Milvus（2天，建表检索）

第3周：AutoGen概念（2天，理解多Agent分工）→ 项目实战（3天，知识库助手）→ 面试题（2天，背熟7道题）

第4周：进阶：LangSmith + 知识图谱 + MCP（了解概念即可）→ 优化项目→模拟面试

核心建议：RAG是绝对核心，吃透它（能独立手写代码+讲清原理），其他模块理解概念+会改Demo即可。面试把RAG项目讲深讲透，足够拿下大多数岗位。
